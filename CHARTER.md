# PAI-Station 治理章程 v1

## 对外承诺三律（Ghost 律）

1. 永不卖用户数据：用户画像/记忆/决策是用户资产，绝不出售、绝不出租、绝不广告化
2. 数据本地优先：markdown/JSONL 明文落地本机，云同步是可选项不是前提
3. 一键带走：export/heritage 全量导出随时可用，数据可携带是退出权

## 开源核心（用户主权不可收费的部分）

- `src/paistation/sovereign`
- `src/paistation/gate`
- `src/paistation/control`
- `src/paistation/market`
- `src/paistation/channels`
- `src/paistation/profile`
- `src/paistation/memory`
- `src/paistation/skills`
- `src/paistation/evolve`

## 商业边界（收费只在托管与增值，不在主权）

- 托管同步（多端 vault 同步服务）
- 托管备份（加密异地备份）
- 多端订阅（家庭/团队席位）
- 验证资产市场抽成（签名 skill card 分发）

## 一致性
- 本章程由 `audit_charter` 机器校验：上述核心目录不存在即章程失效。
