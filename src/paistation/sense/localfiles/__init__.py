"""本地文件宇宙感知（64 项目调研方案书 P0-P4）。

五层：L0 枚举(walk) → L1 提取(triage/extract + 提取缓存) →
L2 语义(桥接 memory.hybrid) → L3 信号分析(signals) → L4 接入
(connector：M2.6 CloudConnector 同构 + MCP 检索)。
索引库落 data/local_index/（gitignored，语料不进 git）。
"""
