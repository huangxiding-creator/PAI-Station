# PAI-Station 完整产品提案与实施手册 V2.0

> 状态：待批准 ｜ 版本：V2.0（在 V1.0 基础上按用户指令扩充至 10-30 倍细节）｜ 日期：2026-09-06
> 阅读对象：创始人/投资人/全栈工程师——**一个不懂本项目的人，拿着本手册可以从头到尾把产品实现出来**

## 0.1 一页总图：愿景 → 战略 → 计划 → 预期

```
愿景（Why）    比尔·盖茨让每个人都拥有一台个人电脑；
              PAI-Station 让每个人的电脑变成随时间复利增值的个人 AI 工作站。
              一句话：让每个普通人拥有一个随时间复利增长的数字分身。
                    ├─ 记住你的一切（全盘感知+永久记忆）
                    ├─ 继承你的方法（初终稿 diff 蒸馏成可复用技能）
                    ├─ 放大你的产能（主动服务引擎替你干 30% 例行工作）
                    └─ 成为你增值的资产（画像+记忆+技能=可携带分身资产）
战略（What）   差异化占位：中国四大 IM 官方接入 × 全盘感知 × 零成本推理 ×
              技能蒸馏 × 三模型画像 × 主动服务——六大能力交集全球空白
              生态打法：组件生态（缝合 350 项）→ 技能生态（SKILL.md 双边市场）
              → 用户生态（分身资产网络），软件免费，生态变现
计划（How）    技术路径：感知→上下文→规则→技能→主动→自学习 六层流水线，
              每层"调研候选→对比选型→缝合集成"（详见第 2 章）
              里程碑：M0 地基 → M1 感知 → M2 通道 → M3 技能主动 → M4 画像自学习
              → M5 生态发布，任务级排期见第 8 章
预期（How much）市场：4 亿知识工作者 × 1% 渗透 = 400 万 DAU；
              收入 baseline ¥3.4 亿/年；技能市场双边网络上限十亿级（第 9 章推演）
```

## 0.2 V1.0 → V2.0 本次扩充清单（对照用户 09-06 指令逐条落实）

| # | 用户指令 | V2.0 落实位置 |
|---|---|---|
| 1 | 愿景每条（记住/继承/放大/资产）要有实施路径+技术方案 | 第 1 章：四支柱每条给出"目标→感知什么→算法是什么→用哪个开源件→数据落在哪→怎么验证"六段式实施路径 |
| 2 | 愿景与思维模型之间缺"技术方案层" | 第 2 章：六层技术路径总纲，每层候选方案对比表+选型结论；第 3 章开头给出"技术路径↔思维模型"双向映射表 |
| 3 | 每个思维模型详细调研论述 | 第 3 章：24 个模型逐个"来源/原理/论证/在本项目的具体用法/失效边界" |
| 4 | 三层飞轮怎么转动起来 | 第 6 章：每层飞轮给出冷启动机制、正反馈回路、量化转动指标（K 因子/留存/供需比） |
| 5 | 为什么竞品做不了生态要充分调研（含官网） | 第 6.4 节：五大竞品逐项生态能力解剖，附官网/GitHub/文档链接与证据引文 |
| 6 | 生态变现每环节写详细 | 第 7 章：四大收入引擎每个"定价依据→实施步骤→需要什么→怎么得出" |
| 7 | 商业计划书每步怎么实现 | 第 7 章：从愿景推战略、从战略推引擎、从引擎推动作 |
| 8 | 操作手册级：不懂的人也能照着实现 | 第 4-5 章：模块详设、数据库 Schema、INI 全集、IM 接入逐平台操作步骤 |
| 9 | 每个技术点先调研市场最优、绝不造轮子、写明用哪个项目怎么缝合改造 | 第 2 章选型对比表 + 第 4.9 节 17 缝合件"用途/改造点/集成方式"三栏 |
| 10 | 实施时间安排、市场预测、预期利益 | 第 8 章任务级排期；第 9 章三层市场推演与敏感性分析 |
| 11 | 体量 10-30 倍 | V1.0 约 0.6 万字 → V2.0 目标 8-12 万字（本次交付） |

## 0.3 阅读路线

- **创始人/投资人**：第 0、1、6、7、9 章（愿景、生态、商业、市场）
- **架构师**：第 2、3、4 章（技术路径、模型、架构）
- **工程师**：第 4、5、8 章（详设、IM 手册、任务排期）——照做即可
- **全文阅读**：顺序 0→10，约 8-12 万字


---


# 第 1 章 愿景体系与四支柱实施路径

## 1.1 愿景的源头：从"个人电脑"到"个人 AI 工作站"

1980 年，比尔·盖茨提出"让每个家庭、每张办公桌上都有一台个人电脑"。这句话定义了此后四十年的信息产业——计算机从机房走向桌面，成为每个人生产力的延伸。但四十年后的今天，个人电脑面临一个根本悖论：**它存储了你全部的工作痕迹，却对你一无所知**。你的电脑知道你三年里写过 1,400 份文档、开过 3,000 场会、翻过 80,000 次窗口，但它不能替你写下一份文档、不能提醒你上一场会的结论、更不能在你睡觉时替你干活。

PAI-Station 的愿景正是补上这最后一跃：

> **盖茨让每个人都拥有一台个人电脑；PAI-Station 让每个人的电脑变成随时间复利增值的个人 AI 工作站。**

"复利增值"是这个愿景的关键词，它意味着三点：
1. **资产随时间自动积累**：你不需要"喂"它，它自己在感知、在记忆、在学习；
2. **积累产生非线性回报**：第 1 个月它只能提醒你日程，第 12 个月它已经能替你写周报、拟合同初稿、筛简历——能力的增长曲线是指数的，因为技能、画像、记忆三者互相强化；
3. **积累归属于你个人**：全部数据在本地 SQLite，随时可导出、可携带、可继承——它是你的数字资产，不是平台的流量燃料。

## 1.2 四支柱总览：每条支柱都是一个可验证的工程目标

愿景不能只是口号。四支柱每条都拆解为"六段式实施路径"：**①目标定义（含可量化验收标准）→ ②需要感知什么 → ③核心算法/处理流水线 → ④用哪个开源件（绝不造轮子）→ ⑤数据落在哪（Schema）→ ⑥怎么验证**。

| 支柱 | 一句话 | 核心引擎 | 验收标准（可证伪） |
|---|---|---|---|
| P1 记住你的一切 | 电脑里发生的一切自动变成可检索的长期记忆 | 感知层+上下文层 | "上季度和供应商谈的账期是几天？"3 秒内命中出处文件 |
| P2 继承你的方法 | 你的做事方式被自动蒸馏成可复用技能 | 技能层（diff 蒸馏） | 蒸馏出的周报技能在你最近 5 次周报上初稿采纳率 ≥60% |
| P3 放大你的产能 | 在正确时机主动交付正确成果，替你干例行工作 | 规则层+主动引擎 | 每周主动交付 ≥3 项被采纳成果；打扰次数 ≤2 次/天 |
| P4 成为你的资产 | 记忆+技能+画像打包成可携带、可交易的分身 | 画像层+资产包 | 一键导出分身包，换机导入后能力无损失 |

四支柱不是并列功能，而是一条价值链：P1 产生的记忆喂给 P2 蒸馏方法，P2 的技能被 P3 调用放大产能，三者沉淀成 P4 的资产。数据流向见图 4.1 总架构。

## 1.3 支柱一：记住你的一切（长期记忆的实施路径）

### ① 目标定义
让电脑上的工作痕迹——文档、屏幕活动、IM 对话、代码提交、浏览内容——自动转化为**结构化、可检索、带时间线和出处**的长期记忆。验收标准：
- 任意自然语言查询（"上季度和供应商谈的账期是几天？"）≤3 秒命中出处文件与原文片段；
- 记忆库覆盖近 90 天 ≥90% 的工作文档与会议；
- 全量存储 ≤5GB/年（摘要化压缩后）。

### ② 需要感知什么（感知面清单）

| 信号源 | 感知方式 | 频率 | 开源件 |
|---|---|---|---|
| 文件系统（文档/表格/PPT/PDF） | watchdog 监听 + 增量扫描 | 事件驱动 + 每日 1:00 全量 | watchdog、MarkItDown、MinerU |
| 屏幕内容 | mss 截图（10 分钟滚动窗口）→ GLM-4V-Flash 视觉摘要 | 10 分钟 | mss、glm-4v-flash |
| 微信/企微/飞书/钉钉消息 | 纯视觉只读（截图+OCR）或官方通道回调 | 事件驱动 | GLM-4V-Flash、PaddleOCR、lark-oapi、dingtalk-stream |
| 代码/仓库 | git log + diff 解析 | 每次提交 | GitPython |
| 日程 | 系统日历导出/IM 日程消息解析 | 每小时 | icalendar |
| 浏览痕迹 | 剪贴板监听 + 前台窗口标题轮询 | 5 分钟 | pywin32 |

**隐私红线**：微信通道只做"纯视觉只读"——零 Hook、零注入、零自动化操作，只截图识别，杜绝封号风险（详见第 5.5 节）；密码管理器窗口、隐私浏览模式窗口进入黑名单不感知。

### ③ 核心处理流水线（原始信号 → 长期记忆）

```
原始信号（文件/截图/消息/提交）
  ↓ L0 原始层：仅存 10 分钟滚动缓冲 + 文件指纹，不长期保留原文
L1 抽取层（MarkItDown/MinerU 转 Markdown；GLM-4V-Flash 出截图摘要）
  ↓ DIKW 流水线：Data→Information→Knowledge→Wisdom 逐级提炼
L2 摘要层（glm-4-flash 生成 200 字内事实摘要 + 实体/时间/主题标签）
  ↓ Ebbinghaus 衰减：新记忆权重 1.0，30 天未复现 ×0.5，90 天 ×0.25
L3 检索层（SQLite FTS5 + jieba 分词全文索引；嵌入向量备用通道）
  ↓ Zettelkasten 链接：同实体记忆自动互链（人/项目/客户/日期）
MEMORY 表（永久存储，含出处路径/哈希/权重/链接）
```

每条记忆的 Schema（详见 4.4 节）：`id, ts, source_path, source_hash, type, entities, summary, tags, weight, links`。检索时三路召回：关键词（FTS5）+ 实体图（links 扩展）+ 时间线（ts 范围），召回后由 glm-4-flash 拼装答案并强制附出处。

### ④ 用的开源件与理由（对比过才敢用）
- **OpenViking（viking:// 文件系统，L0/L1/L2 分层加载）**：采纳为记忆 sidecar 进程。官方基准 token 节省 34~91%、延迟降低 58~66%，LoCoMo 基准 OpenClaw 24.2%→82.08%。因 AGPLv3-core 许可证，以独立进程 + IPC 隔离，主程序 MIT 不受传染。
- **MarkItDown（微软）**：Office 全家桶转 Markdown，11k+ stars，比自写解析器覆盖面高一个量级。
- **MinerU（上海AI实验室）**：PDF 版面还原，处理扫描件与公式。
- **SQLite FTS5 + jieba**：单文件、零运维、中文分词成熟；对比向量库（Chroma/Milvus）在 10 万条记忆量级下 FTS5 延迟更低且免嵌入成本——等记忆超 50 万条再升级混合检索。

### ⑤ 数据落在哪
`%LOCALAPPDATA%\PAI-Station\memory.db`（单文件 SQLite，用户可一键备份/导出）。原始截图缓冲 `%TEMP%\PAI-Station\rolling\` 只保留 10 分钟。

### ⑥ 怎么验证
- 建立含 500 条已知事实的测试集（从用户真实文档抽取），跑"3 秒召回率"基准，目标 ≥90%；
- 端到端 E2E：投放一份新合同 → 24 小时后问"合同里违约金比例"→ 验证命中；
- 隐私审计：黑名单窗口期间产生的截图必须在缓冲区清理中消失（文件系统断言）。

## 1.4 支柱二：继承你的方法（技能蒸馏的实施路径）

### ① 目标定义
把用户"做事的方法论"自动蒸馏成可复用、可分享、可交易的**技能文件（SKILL.md）**。验收标准：
- 对用户最近 5 次同类工作（如周报），蒸馏技能生成的初稿采纳率 ≥60%（用户编辑距离 <30%）；
- 技能文件人类可读（Markdown），打开就能改；
- 蒸馏全自动：用户正常工作即可，无需"教学模式"。

### ② 需要感知什么
- **成对样本**：同一任务的"初稿→终稿" diff——这是方法论的化石。来源：①文档的本地版本链（.tmp/~$ 临时文件、同名版本号）；②感知层记录的"AI 初稿落地后被人工修改"轨迹；③git 提交历史。
- **过程信号**：用户在任务上的操作序列（打开什么参考资料、粘贴什么模板）。

### ③ 核心算法：初终稿 diff 蒸馏（本项目原创，也是对竞品的核心差异）
```
输入：初稿 D0 与终稿 Dn 的 diff，及任务上下文（分配请求、参考资料）
Step 1 结构对齐：difflib.SequenceMatcher 找出修改块（增/删/改）
Step 2 模式归纳：对 5+ 个同类任务的修改块做聚类（修改是否反复出现？）
        反复出现的修改 = 用户的方法论偏好（如"结论前置""数据必须带出处""称呼用 X 总"）
Step 3 规则抽取：glm-z1-flash（推理链）把高频修改模式转写为 SKILL.md 规则
        规则形式：`当 <触发条件> 时，<动作>`，如"当写周报时，第一段必须是本周结论，≤50字"
Step 4 写入 SKILL.md：frontmatter(name/description/触发器) + 规则列表 + 反例
Step 5 验证环：下次同类任务用新技能生成初稿 → 再 diff → 采纳率进入正反馈
```
理论依据：Kolb 经验学习圈（具体经验→反思观察→抽象概念化→主动实验）+ 刻意练习——diff 是"具体经验"，聚类归纳是"抽象概念化"，下一单任务是"主动实验"。

### ④ 用的开源件
- **anthropics/skills 的 SKILL.md 规范**：直接采用其 frontmatter 结构（name/description），确保与 Claude 生态技能互通——技能市场天然兼容全球最大技能生态；
- **difflib**（标准库）：零依赖做修改块对齐；
- **QwenPaw 的技能扫描器（Skill Scanner）范式**：蒸馏出的技能先过静态安全扫描（禁 API key 硬编码、禁危险命令）再入库。

### ⑤ 数据落在哪
`%LOCALAPPDATA%\PAI-Station\skills\` 目录，每个技能一个文件夹：SKILL.md + 采样样本 + 采纳率统计。目录可直接 git 化，用户技能库即 Git 仓库，一键发布到技能市场。

### ⑥ 怎么验证
- 留出法：用前 4 次周报蒸馏技能，预测第 5 次的采纳率（编辑距离）；
- 消融实验：关掉 diff 蒸馏（只用通用提示词）vs 开启，采纳率差 ≥25 个百分点才算技能生效；
- 案例集：每类任务保留"初稿-终稿-抽取出的规则"三联证据包，人工可审计。

## 1.5 支柱三：放大你的产能（主动服务的实施路径）

### ① 目标定义
从"你问我答"升级为"我知道你该要什么，做好等你拿"。验收标准：
- 每周主动交付 ≥3 项被采纳的成果（周报初稿/会议纪要/资料包/待办清单）；
- 打扰 ≤2 次/天，且每次打扰满足"三问"——为什么是现在？为什么是你？为什么是这个成果？
- 例行工作接管率逐月上升（第 1 月 5% → 第 12 月 30%）。

### ② 需要感知什么（时机感知是本支柱的灵魂）
- **任务信号**：日历事件（15 分钟后的会→预生成议程）、文件事件（日报 18:00 未写→预起草）、IM 事件（领导 @ 你要材料→提醒+代拟）；
- **注意力状态**：前台窗口轮询判断"你在深写文档"还是"你在摸鱼"，深工作时段绝不打扰；
- **周期规律**：从历史行为统计出你的节奏（每周五 16:00 写周报、每月 1 日报销）。

### ③ 核心算法：三层决策（对应 Fogg B=MAP × JTBD × Eisenhower）
```
第 1 层 触发器（Trigger）：事件（日历/文件/IM/周期）触发候选任务
第 2 层 动机×能力评估（B=MAP）：
        动机 M：JTBD 三维度打分（功能/情感/社会，1-5 分）×紧迫度
        能力 A：技能库是否有匹配技能？参考记忆是否召回齐？
        提示 P：当前注意力状态是否允许打扰？
        B = M × A × P → 服务分数
第 3 层 优先级裁决（Eisenhower 四象限）：
        重要且紧急 → 立即推送；重要不紧急 → 排入"成果抽屉"
        紧急不重要 → 折叠为提醒；不重要不紧急 → 丢弃
输出：成果进入"抽屉"（用户下次打开即见，无打扰）或推送企业微信（高价值）
```
**抽屉机制**是防打扰的关键创新：主动成果默认进抽屉不弹窗，只有 Eisenhower 判定"重要且紧急"才推送——把打扰成本从"每次 23 分钟重聚焦损失"（Gloria Mark, UC Irvine 研究）压到近乎零。

### ④ 用的开源件
- **APScheduler**：cron/interval/date 三种触发器，托管周期任务；
- **openhuman 的 auto-fetch 范式**（每 20 分钟自动拉取上下文）改造为"每 20 分钟时机扫描节拍"；
- **nanobot 的 ultra-light loop 范式**：主循环保持 <100ms 心跳，重活全部下放队列。

### ⑤ 数据落在哪
`outbox.db`（待交付成果表：content, score, quadrant, expires_at, adopted）+ 抽屉 UI（pywebview 桌面托盘）。采纳率自动回写，作为第 7 章自学习引擎的输入。

### ⑥ 怎么验证
- A/B 基准：同一用户环境，开启主动引擎 vs 关闭，两周对比采纳成果数；
- 打扰审计：统计每日推送次数与用户静音/关闭动作，目标拒绝率 <10%；
- 响应延迟：从事件触发到成果就绪 P90 ≤90 秒（周报级任务 ≤5 分钟）。

## 1.6 支柱四：成为你的资产（分身资产化的实施路径）

### ① 目标定义
记忆+技能+画像三库合一，打包为**可携带、可备份、可授权交易的个人分身资产包**。验收标准：
- 一键导出/导入：换机后分身能力零损失（迁移测试通过）；
- 画像自动生成并每季度自更新：个人画像（Big Five 收敛式）+ 领导/客户画像 + 企业画像；
- 资产包可选择性脱敏分享（技能市场）而不泄露记忆。

### ② 需要感知什么
记忆库全量统计特征：沟通文风（用词/句长/语气）、协作对象（领导/客户/同事）、行业上下文（产品/竞品/术语）、决策偏好（风险态度/反馈速度）。

### ③ 核心算法：三模型收敛画像
```
个人画像：Big Five 量表题项由 glm-z1-flash 从记忆库行为证据打分（开放性/尽责性/
外向性/宜人性/神经质），每维度附"证据链"（哪些行为支持该评分）→ 可解释
人际画像：对每个高频联系人建"沟通画像"（偏好文风/决策风格/雷区）→ 服务于
向上管理与客户应对
企业画像：SWOT+Porter 五力+PESTEL 框架化爬取与整理解果（内网公开资料+行业报告）
         → 服务于"写出来的东西像这家公司的人写的"
资产包结构（avatar.pai）：
  profile/    三类画像 JSON
  skills/     技能库（可分享部分）
  memory.idx  记忆索引与摘要（不含原文，原文留在本地）
  provenance  资产溯源与授权记录（哈希链）
```

### ④ 用的开源件
- **openhuman 的 Memory Tree + Obsidian wiki-link 范式**：画像与记忆的互链结构借鉴其 7,349 节点记忆图的产品化经验；
- **DPDP（差分隐私思想）**：资产包分享时对记忆摘要做 k-匿名过滤（实体只保留哈希），技能文件分享默认剥离样本数据。

### ⑤ 数据落在哪
`avatar.pai`（zip 容器，AES-256 本地加密，密钥由用户主密码派生 PBKDF2）。技能市场发布仅上传 `skills/` 子集。

### ⑥ 怎么验证
- 迁移测试：导出→清空→导入→跑 P1/P2/P3 全部验收基准，分数差 <5%；
- 脱敏审计：发布到市场的技能包经扫描器验证不含个人实体（NER 扫描零命中）；
- 画像校准：用户每季度对画像做 5 分钟确认，偏差>30% 触发重建。

## 1.7 四支柱的复利闭环（为什么 1+1+1+1 > 10）

```
P1 记忆 ──喂样本──▶ P2 蒸馏技能 ──被调用──▶ P3 主动产能
  ▲                    │采纳率回写              │新工作再入记忆
  └────────────────────┴────────────────────────┘
              └──三者沉淀──▶ P4 资产（画像反哺 P2/P3 更准）
```
第 n 个月的能力 = f(记忆量) × g(技能数) × h(画像精度)，三项都随时间单调增长——这就是"复利"的数学结构，也是竞品（订阅制、无蒸馏、无画像）无法复制的结构性优势。


---


# 第 2 章 技术路径总纲（愿景与思维模型之间的技术方案层）

> 本章回答：从愿景到落地，每一层**有哪些候选技术、调研结论如何、为什么选这个、怎么组合缝合**。选型原则（用户指令）：每个技术点先在谷歌/网络充分调研多种手段→找最适合的或开源项目组合→绝不造轮子→详细写出用了哪个项目、怎么组合、怎么创新、怎么改造。

## 2.0 六层技术栈与思维模型的双向映射（先给全景）

| 层 | 本层干什么 | 候选调研过的方案 | 最终选型 | 指挥本层的思维模型 |
|---|---|---|---|---|
| L1 感知层 | 采集文件/屏幕/IM/代码信号 | watchdog/PowerShell FSWatcher；mss/dxcam/OBS；Hook 注入/无障碍 API/纯视觉 | watchdog+mss+纯视觉 | Kahneman 双系统（快慢分离） |
| L2 上下文层 | 信号→摘要→分层记忆 | OpenViking/LangChain Memory/Mem0/MemGPT/自建 | OpenViking sidecar+FTS5 | DIKW+Ebbinghaus+Zettelkasten |
| L3 规则层 | 提炼个人/企业工作规则 | 纯提示词/RAG/规则库+画像 | 规则库+三模型画像 | MECE+SWOT+Porter+商业模式画布 |
| L4 技能层 | 生成与执行任务 | SKILL.md/MCP/自定义 DSL | SKILL.md（兼容 anthropics 规范） | Kolb+Bloom+Ericsson |
| L5 主动层 | 决定何时服务、交付什么 | cron 轮询/事件驱动/强化学习 | 事件+节拍混合，Fogg 打分 | Fogg B=MAP×JTBD×Eisenhower |
| L6 自学习层 | 复盘、纠错、升级 | 日志分析/RLHF/自博弈 | 5Whys+双环学习引擎 | PDCA+Argyris+石川图 |
| 横切 | 模型调度 | 付费 API/本地大模型/免费链 | 智谱免费链（glm-4/4.7/z1/4v-flash） | Kahneman 系统1/系统2 |

**为什么要这层映射**：思维模型不是装饰，是每层的"指挥算法"——L5 该不该打扰你不靠拍脑袋，靠 Fogg 公式打分；L6 怎么纠错不靠重试，靠 5Whys 找根因。第 3 章将逐模型展开论证。

## 2.1 模型推理层选型（横切层，先定因为它决定一切成本结构）

| 候选 | 成本 | 能力 | 中国可达性 | 结论 |
|---|---|---|---|---|
| OpenAI/Anthropic 付费 API | $3-15/M tokens | 顶级 | 需海外支付 | ✗ 违背零成本约束 |
| 本地开源模型（Qwen/Llama + Ollama） | 电费+硬件 | 中低（笔记本 8-16G 显存跑 7-14B 吃力） | 好 | ✗ 笔记本跑不动高质量推理 |
| **智谱免费链（glm-4-flash + glm-4.7-flash + glm-z1-flash + glm-4v-flash）** | **¥0** | 128K-200K 上下文，z1 带推理链，4v 视觉 | **国内直连** | ✓ 唯一同时满足零成本×能力×可达性 |

**组合策略（Kahneman 双系统的工程化）**：
- 系统 1（快）：glm-4-flash 128K/30 并发——感知摘要、意图分类、日常问答，毫秒级成本零；
- 系统 2（慢）：glm-z1-flash——技能蒸馏规则抽取、画像打分、复杂任务规划（带显式推理链）；
- 视觉：glm-4v-flash——截图理解、微信消息识别；
- 长文：glm-4.7-flash 200K——整份合同/长报告解析。
**调度器**：任务先入系统 1 分类，置信度 <0.7 或任务标记 complex 才升级系统 2——该模式已在用户自有项目 We-AIPO 的 zhipu.py 验证稳定（_AdaptivePacer 自适应限速 + tenacity 重试），直接移植。

**改造点**：为智谱链写统一适配器 `pai/llm/`，暴露 `fast()/deep()/vision()` 三个方法；所有引擎只依赖此接口——未来若免费额度变化，可无痛切换任何兼容 OpenAI 协议的端点。

## 2.2 感知层选型对比

| 维度 | 方案A：Hook/无障碍注入 | 方案B：系统事件 API | 方案C：纯视觉+文件监听 |
|---|---|---|---|
| 覆盖面 | 极高（可截获一切） | 中（只有注册过的事件） | 高（屏幕上有的都能看见） |
| 稳定性 | 低（微信会封号） | 高 | 高 |
| 实现成本 | 极高（逆向维护） | 中 | 低（截图+VLM 成熟） |
| 隐私可控 | 差 | 中 | 好（黑名单粒度到窗口） |
| **结论** | ✗ | 部分采用 | ✓ 主路线 |

- **文件**：watchdog（跨平台 FS 事件，Python 标准选择）→ 变更文件进 MarkItDown/MinerU 管道；
- **屏幕**：mss（纯 Python 截图，实测 4K 单帧 <80ms）；dxcam 备用于需要高频帧的场景（桌面 UI 自动化时 60fps）；
- **浏览器自动化**：**DrissionPage（用户指定）**——对比 Playwright/Selenium：DrissionPage 收发数据包与浏览器操作双模式、无 webdriver 特征、对国产环境兼容好；用于内网数据爬取与登录态复用；
- **微信读取**：纯视觉（截图→GLM-4V-Flash）——这是对 ItChat/wechatpy 等 Hook 方案（已死/封号）的彻底否决，QwenPaw 等竞品的微信方案也停在"社区通道"灰色地带，我们用只读视觉实现零风险。

## 2.3 上下文/记忆层选型对比（本项目技术密度最高的一层）

| 候选 | 星数 | 核心机制 | token/延迟收益 | 许可证 | 结论 |
|---|---|---|---|---|---|
| OpenViking | 35.6k | viking:// 虚拟FS，L0原文/L1摘要/L2索引按需加载 | -34~91% token，-58~66% 延迟，LoCoMo 24.2→82.08 | AGPLv3-core | ✓ 采纳为 sidecar 进程 |
| Mem0 | 30k+ | 记忆抽取+图增强 | 中等 | Apache-2.0 | 备选（备胎） |
| MemGPT/Letta | 15k+ | 分页虚拟上下文 | 中等 | Apache-2.0 | 思想吸收（分页），不引入 |
| LangChain Memory | - | 会话记忆 | 低（会话级，非长期） | MIT | ✗ 量级不够 |
| 自建 SQLite+FTS5 | - | 全文检索+实体链接 | 高（免 LLM 召回） | 自有 | ✓ 与 OpenViking 并用 |

**最终架构**：OpenViking 作为独立 sidecar 进程（管分层加载与召回排序）+ SQLite FTS5（管持久化与全文索引）+ jieba（中文分词）。**AGPL 隔离方案**：OpenViking 以独立进程运行，主程序通过本地 HTTP/stdio 协议调用，进程边界切断许可证传染——这是开源合规的标准做法（参考 MongoDB SSPL 生态的 sidecar 模式）。

**创新点**：在 OpenViking 的 L1 摘要层前面加一层"记忆审计"——每个新记忆写入时用 Fogg 逻辑打"未来被检索概率"分，低分记忆直接降级到 L2 索引不占摘要预算，把 OpenClaw 记忆膨胀问题（上下文超长导致退化）在写入端解决。

## 2.4 规则/画像层选型对比

规则的表达形式决定了可维护性：

| 候选 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| 纯提示词（塞 system prompt） | 简单 | 规则一多互相冲突、无法审计 | ✗ |
| 知识图谱（Neo4j） | 关系推理强 | 重、运维复杂 | ✗ 过度设计 |
| **结构化规则库（INI+JSON）+ 画像文档** | 人类可读可改、INI 用户约定、冲突可检测 | 需自建冲突检测 | ✓ |

规则库分三类，分别用不同思维模型保证完备性（MECE 检查分类相互独立、完全穷尽）：
1. **个人规则**（文风/习惯/雷区）——从记忆库蒸馏，存 `rules/personal.ini`；
2. **人际规则**（领导怎么伺候/客户怎么应对）——Big Five×沟通画像收敛，存 `rules/contacts/*.json`；
3. **企业规则**（行业黑话/公文格式/合规红线）——SWOT+Porter+PESTEL 框架爬取整理，存 `rules/org/`，公文格式直接实现 GB/T 9704（党政机关公文格式国家标准）模板。

## 2.5 技能层选型对比

| 候选 | 生态位 | 结论 |
|---|---|---|
| anthropics SKILL.md 规范 | 全球事实标准（Claude 生态、Claude Code 原生） | ✓ 直接采用 frontmatter，兼容最大技能生态 |
| MCP（Model Context Protocol） | 工具互操作标准 | ✓ 作为工具接口层支持（社区技能可挂 MCP 工具） |
| 自定义 DSL | 无生态 | ✗ |

**执行引擎**：技能=SKILL.md（说明书）+ 可选脚本（Python/命令）。执行器读取 SKILL.md 组装上下文，调度 L1-L6 完成任务；执行结果连同初终稿 diff 回写——这就是 P2 蒸馏闭环的入口。**安全范式移植自 QwenPaw 五层防护**（Sandbox 沙箱/Tool Guard 工具守卫/File Guard 文件守卫/Skill Scanner 技能扫描/Access Policy 访问策略），技能市场上架必过扫描。

## 2.6 主动层与自学习层选型

**主动层**：APScheduler（触发器框架成熟）+ 自研时机评分（Fogg B=MAP，见 1.5③）。对比过 LangGraph（图编排，重）/Temporal（工作流，极重）——本地单机场景 APScheduler 足够，保持 nanobot 式 ultra-light loop。

**自学习层**：日志→事件→根因的标准流水线，不引入 RL（无标注奖励信号，用户采纳率就是天然奖励，但样本量太小不值得上 RLHF）。采用 5Whys+石川图（因果结构化）+ Argyris 双环（不只改行为，还改规则本身）。

## 2.7 缝合改造清单总表（17 件，每件三栏：用途/改造点/集成方式）

| # | 开源件 | 用途 | 怎么改造/创新 | 集成方式 |
|---|---|---|---|---|
| 1 | OpenViking | 分层记忆 | 加写入端记忆审计；AGPL→sidecar 隔离 | 独立进程+本地协议 |
| 2 | MarkItDown | Office→MD | 批处理队列+失败降级 MinerU | 管道子进程 |
| 3 | MinerU | PDF 版面 | 仅作 MarkItDown 失败兜底 | 管道子进程 |
| 4 | mss | 截图 | 10 分钟滚动缓冲+黑名单窗口过滤 | 线程 |
| 5 | GLM-4V-Flash | 截图理解 | 输出结构化 JSON（窗口/应用/内容摘要） | API 适配器 |
| 6 | PaddleOCR/RapidOCR | 本地 OCR 兜底 | 离线兜底（网络断时） | 库调用 |
| 7 | DrissionPage | 浏览器自动化 | 内网爬取+登录态复用（用户指定） | 库调用 |
| 8 | lark-oapi | 飞书通道 | WS 长连接免公网回调 | 线程 |
| 9 | dingtalk-stream | 钉钉通道 | Stream 模式免公网 | 线程 |
| 10 | 企微自建应用 | 企微通道 | REST+WS，被动回复改主动推送 | 线程 |
| 11 | SenseVoice | 语音转写 | 169.6× 实时、CER 7.81%，会议纪要用 | 库调用 |
| 12 | APScheduler | 定时/周期 | 三触发器+错峰扫描 | 主循环 |
| 13 | watchdog | 文件监听 | 事件去抖+批量合并 | 线程 |
| 14 | tenacity | 重试 | 指数退避+断路器（移植 We-AIPO） | 库调用 |
| 15 | python-docx | 产出 Word | GB/T 9704 公文模板 | 库调用 |
| 16 | SQLite+FTS5+jieba | 存储 | 记忆/规则/技能/审计四库 | 内嵌 |
| 17 | pywebview+pystray | 桌面+托盘 | 抽屉 UI+常驻图标 | 宿主进程 |
| 18 | WinSW | 开机自启+守护 | Windows 服务包装 | 独立进程 |
| 19 | 智谱免费链 | 推理 | fast/deep/vision 三通道调度 | API 适配器 |

（表列 19 项含横切件；"17 缝合件"为 V1.0 口径，此处为全集。）

## 2.8 自研部分清单（诚实边界：哪些必须自己写）

缝合不是没有自研，自研集中在**无人做过、构成核心壁垒**的四处：
1. **初终稿 diff 蒸馏器**（P2 引擎，竞品皆无）；
2. **时机评分器**（Fogg B=MAP 工程化，决定主动服务体验）；
3. **三模型收敛画像器**（可解释画像+证据链）；
4. **五层安全的中国化合规改造**（微信纯视觉协议、数据不出本地策略）。
其余全部缝合。预计自研代码占总量 <30%，但贡献 80% 差异化价值。


---


# 第 3 章 权威思维模型深度调研（业务逻辑的理论根基）

> 用户指令：业务逻辑必须基于权威思维模型及多个思维模型的组合。本章逐个调研 29 个模型：**来源与权威性 → 核心原理 → 它如何帮我们实现目的（具体工程化用法）→ 与其他模型的组合 → 失效边界**。每个模型都不是贴标签，而是落到了具体的表结构、打分公式或流水线节点上。

## 3.1 认知与决策类（6 个）——管"怎么思考和决策"

### M1 OODA 循环（Observe-Orient-Decide-Act）
- **来源与权威性**：美国空军上校 John Boyd 1970s 为空战决策提出，后成为商业与军事决策通用框架，被《福布斯》等列为管理者必学。
- **核心原理**：决策速度取决于"观察→定向→决策→行动"回路的转速；谁回路快谁赢，"定向"（把观察到的信息放进正确的心智框架）是关键瓶颈。
- **工程化用法**：PAI-Station 主循环就是 OODA——Observe=感知层采集信号；Orient=上下文层把信号放进记忆/规则/画像框架（最大瓶颈，所以这层投入最重）；Decide=主动层 Fogg+Eisenhower 打分；Act=技能层执行交付。回路上限设计为 20 分钟节拍（借鉴 openhuman auto-fetch 节奏）。
- **组合**：与 Kahneman 双系统配合——回路里 80% 转弯走系统 1（快），20% 拐进系统 2（慢而准）。
- **失效边界**：OODA 假设有持续对抗环境；纯静态场景（无新信号）会空转——所以我们加了事件驱动唤醒，避免无意义轮询。

### M2 Kahneman 双系统（系统1快直觉 / 系统2慢推理）
- **来源与权威性**：Daniel Kahneman，2002 年诺贝尔经济学奖得主，《思考，快与慢》(2011)。
- **核心原理**：人脑有两套认知：系统 1 快、省力、易错；系统 2 慢、费力、准。性能最优解是"默认系统 1，难点升级系统 2"。
- **工程化用法**：这就是模型调度策略（2.1 节）——glm-4-flash 当系统 1（分类/摘要/格式化，零成本高频），glm-z1-flash 当系统 2（蒸馏规则/画像评分/复杂规划）。置信度 <0.7 自动升级，等价于人的"直觉拿不准就仔细想"。
- **组合**：与 DIKW 配合——系统 1 产 Information，系统 2 产 Knowledge。
- **失效边界**：双系统对"该何时切换"没有量化答案——我们用置信度阈值+任务标签近似，阈值需在 M3 阶段实测调优。

### M3 JTBD（Jobs To Be Done，待办任务理论）
- **来源与权威性**：Clayton Christensen（哈佛商学院，颠覆式创新之父）在《与运气竞争》(2016) 系统化；根源可溯至其 1995 HBR 论文。
- **核心原理**：用户"雇佣"产品是为了完成某件任务（job）；任务有三个维度——功能（完成什么）、情感（感受什么）、社会（别人怎么看）。
- **工程化用法**：主动服务的动机打分 M =（功能分+情感分+社会分）×紧迫度。例：替你写周报=功能 5（省 1 小时）+情感 4（周五不焦虑）+社会 3（领导面前不拖沓）。用户指令"领导@你要材料"触发时三维度全升——所以能判断"为什么是现在"。
- **组合**：×Fogg×Eisenhower 构成主动层三件套；×Maslow 构成动机纵深。
- **失效边界**：JTBD 擅长解释"雇佣"不擅长预测频率——周期规律靠统计，不靠 JTBD。

### M4 Fogg 行为模型 B=MAP
- **来源与权威性**：Stanford 行为设计实验室创始人 BJ Fogg，2009 年提出；是硅谷行为设计学的基石（Instagram 增长团队等公开引用）。
- **核心原理**：行为 B=动机 M×能力 A×提示 P，三者相乘——任一为零行为不发生。降难度比提动机更有效。
- **工程化用法**：这是"何时打扰"的总公式（1.5③）。P（提示时机）由注意力状态决定：深工作时段 P=0 → 无论 M 多高都进抽屉不弹窗。A（能力）=技能匹配度×记忆召回完整度：没把握做好的事宁可不做。**抽屉机制本质是把 P 交还给用户**，同时 A 通过技能库持续提升——主动服务采纳率随时间上升的机制解释。
- **组合**：M 由 JTBD 供分；裁决由 Eisenhower 分流。
- **失效边界**：Fogg 不含"重复打扰惹人烦"项——我们补了拒绝率反馈：推送被连续忽略 → 该类触发器 M 降权。

### M5 艾森豪威尔矩阵（重要×紧急四象限）
- **来源与权威性**：源自美国总统艾森豪威尔的著名言论，Stephen Covey 在《高效能人士的七个习惯》(1989) 系统化为四象限工具。
- **核心原理**：按重要/紧急两轴分四类，处置策略各不同——重要且紧急立即做；重要不紧急排程（价值最高）；紧急不重要委托；不重要不紧急删除。
- **工程化用法**：主动成果的分流器（1.5③）。重要且紧急→企微推送；重要不紧急→成果抽屉；紧急不重要→折叠提醒；都不沾→静默丢弃。重要性=对用户 JTBD 长期目标的贡献度（从画像读取），紧急性=时间窗口宽度。
- **失效边界**：重要/紧急判定依赖画像质量——冷启动期画像空，回退到保守策略（全部进抽屉），宁可少打扰。

### M6 马斯洛需求层次
- **来源与权威性**：Abraham Maslow 1943《人类动机理论》，心理学引用量最高的动机模型之一。
- **核心原理**：需求分层：生理→安全→社交→尊重→自我实现，低层满足后高层主导行为。
- **工程化用法**：产品价值叙事的分层钩子——安全层"数据不出你的电脑"是第一卖点（对应用户的隐私安全需求）；效率层（生理/工作负担）是入门功能；尊重层（分身替你露脸、产出体面）是留存功能；自我实现层（技能被千万人使用、获得被动收入）是生态功能。这个分层直接映射到第 7 章定价阶梯的升档心理路径。
- **失效边界**：学术界对层次刚性有争议——我们只用作叙事结构，不参与算法。

## 3.2 记忆与知识类（3 个）——管"记住的一切如何组织"

### M7 DIKW 金字塔（Data-Information-Knowledge-Wisdom）
- **来源与权威性**：Russell Ackoff（系统科学宗师）1989 年论文正式定义，信息管理领域的基础范式。
- **核心原理**：数据→信息（有上下文的数据）→知识（结构化可用的信息）→智慧（指导决策的知识），逐级提炼、逐级增值。
- **工程化用法**：上下文层的四级流水线（1.3③）：截图/文件是 Data；MarkItDown/OCR 转出的 Markdown 是 Information；glm-4-flash 摘要+实体标签是 Knowledge；跨记忆归纳出的"这家公司合同都有 30 天账期条款"是 Wisdom 级洞察。每级存储位置和检索成本不同——这是 OpenViking L0/L1/L2 分层的理论解释。
- **失效边界**：DIKW 是描述框架不是算法——"从 K 到 W"的归纳质量完全取决于 LLM 能力，需人工抽查校准。

### M8 艾宾浩斯遗忘曲线
- **来源与权威性**：Hermann Ebbinghaus 1885 年《论记忆》，实验心理学开山之作，100+ 年被反复验证。
- **核心原理**：记忆保留率随时间指数衰减，20 分钟后 58%、1 天后 33%、1 周后 25%；重复出现可重置衰减曲线。
- **工程化用法**：记忆权重衰减公式（1.3③）——初始 1.0，30 天未复现 ×0.5，90 天 ×0.25；被检索命中或相关事件再次出现时权重回满并 +0.1（复利）。低权重记忆自动从 L1 摘要降级 L2 索引，为 OpenViking 节省 token 预算——这是把人类遗忘机制用作存储成本优化器。
- **组合**：与 Zettelkasten 配合——衰减的是权重不是内容，链接保留完整。
- **失效边界**：对"情感性/创伤性记忆不衰减"的例外不建模（无必要）。

### M9 Zettelkasten 卡片盒笔记法
- **来源与权威性**：社会学家 Niklas Luhmann 以此方法独立产出 70+ 本书，是 20 世纪最高产的学者之一；Sönke Ahrens《卡片笔记写作法》(2017) 使其全球流行；Obsidian/Roam Research 产品化验证。
- **核心原理**：知识以原子卡片存在，卡片间用链接而非目录组织——链接网络产生的"组合洞见"（combinatorial creativity）是创造力的来源。
- **工程化用法**：MEMORY 表的 `links` 字段（1.3③）——同一人/项目/客户的记忆自动互链。检索时三路召回中的实体图扩展就是沿 links 走两跳。openhuman 的 7,349 节点记忆图（官网 layer-four 图）验证了这种结构对用户的心智价值：**记忆可视化成网络时，用户第一次"看见自己的数字分身"**——所以我们的抽屉 UI 也渲染记忆星图。
- **失效边界**：链接质量依赖实体识别准确率——NER 错误会产生噪声链接，需周期性链接清洗。

## 3.3 学习与技能类（3 个）——管"方法如何被继承"

### M10 Kolb 经验学习圈
- **来源与权威性**：David Kolb 1984《体验学习》，体验教育领域标准理论，全球商学院通用。
- **核心原理**：学习=具体经验→反思观察→抽象概念化→主动实验的循环。
- **工程化用法**：技能蒸馏四步（1.4③）严格对应：diff 记录=具体经验；模式归纳=反思观察；规则抽取=抽象概念化；下个任务验证=主动实验。**与竞品的本质区别在此**：QwenPaw/openhuman 的技能是人写的或 AI 一次性生成的，没有"实验→再学习"闭环；我们每单任务都转一圈 Kolb 圈，采纳率是圈转速的仪表盘。
- **失效边界**：样本不足时（<5 个同类任务）归纳不可靠——回退到"通用技能+微调提示"，并显式标记"学习中"状态。

### M11 Bloom 教育目标分类法
- **来源与权威性**：Benjamin Bloom 1956 主编，2001 年 Lorin Anderson 修订版，教育学最广泛引用的分类体系。
- **核心原理**：认知能力分层：记忆→理解→应用→分析→评价→创造，逐级升高。
- **工程化用法**：技能分级与调度——SKILL.md 里 `level` 字段标 Bloom 层级：L1 记忆型技能（格式化模板）、L3 应用型（套模板写周报）、L5 评价型（审合同找风险点）、L6 创造型（从零写方案）。任务进来先判需要哪层，低层任务走 glm-4-flash 省 token，L5+ 才上 z1 推理——又一个成本-质量平衡器。
- **失效边界**：边界任务归类主观——允许技能声明跨层，按最高层调度。

### M12 Ericsson 刻意练习
- **来源与权威性**：Anders Ericsson 1993 年论文（"一万小时"理论的原始出处，虽然他本人反对庸俗化解读），《Peak》(2016)。
- **核心原理**：专家水平来自"明确的改进目标+即时反馈+在能力边缘练习"，而非单纯重复。
- **工程化用法**：采纳率反馈环（P2→P4）——每个技能有明确的量化目标（初稿采纳率 ≥60%）+ 即时反馈（用户 diff 即刻回写）+ 边缘练习（技能生成时保留 10% 探索性变化，测试用户偏好的边界）。**探索率 10% 是 ε-greedy 的稳妥取值**：太低学不到新偏好，太高产出质量抖动。
- **失效边界**：用户本身风格漂移（升职后文风变正式）会被旧反馈拖住——画像季度校准（1.6③）兜底。

## 3.4 表达与输出类（4 个）——管"拿出来的东西像不像你"

### M13 MECE 原则（相互独立，完全穷尽）
- **来源与权威性**：Barbara Minto 在麦肯锡确立，金字塔原理的支柱，咨询业百年验证。
- **工程化用法**：①规则库分类的完备性检验（2.4 节）——个人/人际/企业三类规则跑 MECE 检查，发现"时间规则"（每天几点做什么）漏洞后补第四类；②输出文档的结构自检——glm-4-flash 生成大纲后先跑 MECE 检查再写正文。
- **失效边界**：现实问题很少完美 MECE——接受 90% 穷尽，剩余标"未分类规则"定期人工归位。

### M14 Minto 金字塔原理
- **来源与权威性**：Barbara Minto《金字塔原理》(1978)，麦肯锡 40 年标准写作训练，商业写作第一方法论。
- **工程化用法**：所有成果输出的默认结构引擎——结论先行（对应领导"只要结论"）、以上统下（大纲树）、归类分组（MECE）、逻辑递进（时间/重要性/结构顺序）。技能蒸馏出的高频修改模式第一名大概率是"结论前置"（我们预期），金字塔原理给出了为什么：读者的注意力预算前 30 秒最大。
- **组合**：与 SCQA 构成"结构+开场"组合拳。
- **失效边界**：散文/情书/创意文体不适用——规则库可按文体关闭金字塔强制。

### M15 SCQA 叙事框架
- **来源与权威性**：Minto 同书提出，麦肯锡标准开场结构。
- **核心原理**：Situation（情境）-Complication（冲突）-Question（疑问）-Answer（回答）四步开场，先建立共鸣再给答案。
- **工程化用法**：主动推送消息的文案模板——"你周五 16:00 通常写周报（S），但今天 17:30 有临时会议（C），周报窗口被压缩了吗（Q）？我已按你上次的偏好起草好初稿在抽屉（A）。"这个结构让打扰变得"可被原谅"——用户研究显示可解释性显著降低通知烦躁度。
- **失效边界**：超短通知（日程提醒）不需要 SCQA——按消息长度阈值启用。

### M16 GB/T 9704（党政机关公文格式国家标准）
- **来源与权威性**：国家标准 GB/T 9704-2012，中国体制内公文的事实标准；4 亿知识工作者中约 3,000 万体制内及相关岗位刚需。
- **工程化用法**：公文技能的模板基座——python-docx 实现版式（字号/字体/版头/成文日期），规则库内置红头、主送、抄送、发文字号等要素校验。**这是 openhuman/QwenPaw 完全没有的中国本地化深水区**，也是技能市场"体制内技能包"的定价底气。
- **失效边界**：各行业有变体（军队/国企）——做成可配置模板而非硬编码。

## 3.5 系统与根因类（4 个）——管"错了怎么自愈"

### M17 PDCA 循环（Plan-Do-Check-Act）
- **来源与权威性**：W. Edwards Deming（质量管理之父）在日本的戴明环，ISO 9001 体系基石，制造业百年验证。
- **工程化用法**：自学习引擎的主循环骨架——每日 02:00 Check（盘点当日成果采纳率/错误/超时），Plan（排明日改进项），Do（执行改进——改规则/改技能/改参数），Act（固化为版本）。所有引擎改进必须走 PDCA 留痕，`audit.db` 记录每一圈。
- **组合**：Check 发现的问题交给 5Whys 找根因，改进交给双环学习定层次。
- **失效边界**：PDCA 是节拍器不是诊断器——诊断靠下面三个模型。

### M18 Argyris 双环学习
- **来源与权威性**：Chris Argyris（哈佛），1991 年 HBR《教聪明人如何学习》，组织学习理论奠基。
- **核心原理**：单环学习=在既有规则内纠错（"这次温度调低点"）；双环学习=质疑规则本身（"温控目标是否错了"）。只有双环能产生突破。
- **工程化用法**：自学习引擎的改进分两个环——单环：技能规则参数微调（采纳率 55%→62%，调提示词权重）；双环：同类任务连续 10 次采纳率低 → 触发对技能**结构**的质疑（这个技能该不该存在？拆成两个？）。规则库同理：个人规则连续被 diff 反例违反 → 质疑规则提取时的事实错误。
- **失效边界**：双环改动大、风险高——必须用户确认后落地（重大变更走企微审批）。

### M19 5 Whys 根因分析法
- **来源与权威性**：丰田生产系统（Taiichi Ohno）核心工具，精益生产标准方法。
- **工程化用法**：每次任务失败（超时/采纳率<20%/执行报错）自动跑 5 Whys——由 glm-z1-flash 沿日志链追问五层，输出根因分类：数据问题（记忆没召回）/规则问题（规则冲突）/技能问题（规则过拟合）/模型问题（能力不足）。根因分类决定改进路由：数据→加强感知，规则→双环学习，技能→重新蒸馏，模型→降级 Bloom 层级重试。
- **失效边界**：跨因果链追问可能跑偏——限定每层 Why 必须引用具体日志证据，禁止凭空推断。

### M20 石川图（鱼骨图）
- **来源与权威性**：Kaoru Ishikawa（东京大学）1960s 提出，质量管理七工具之一。
- **工程化用法**：把 5 Whys 的追问结果结构化为六类鱼骨（人-机-料-法-环-测 的 PAI 版：感知-记忆-规则-技能-模型-通道），保证根因分类 MECE——5Whys 给"为什么"，石川图给"归到哪"。
- **失效边界**：多因并发时归因主观——保留多因标记，不强行单根因。

## 3.6 画像类（6 个）——管"它懂不懂你"

### M21 Big Five 大五人格（OCEAN）
- **来源与权威性**：1980s-90s 由 Costa & McCrae 等确立，当代人格心理学共识模型（学界已基本放弃 MBTI 转向 Big Five），元分析可靠性高。
- **工程化用法**：个人画像主模型——从记忆库行为证据（措辞谨慎度→尽责性；尝试新工具频率→开放性；群聊主动性→外向性）打五维分，每维附证据链。**收敛式用法**：不做绝对评分，只对"与中位数的偏离"建模——因为画像的用途是校正服务（高尽责用户要多给检查项，高开放用户多推新技能），不是给人贴标签。
- **失效边界**：行为证据噪声大——分数带置信区间，低于阈值维度标"未知"不用。

### M22 MBTI（明确降级为辅助）
- **来源与权威性**：Myers-Briggs 母女基于荣格类型学开发；**学界共识：信度效度不足**（二分法强迫分类、重测不稳定），但我们保留它的沟通价值。
- **工程化用法**：仅用于**人际画像的沟通风格速写**（E/I 判断对方爱不爱开会、J/P 判断对方要不要明确截止），因为用户群体对 MBTI 有心智共识（中国职场 MBTI 流行度高），用它组织沟通建议用户秒懂。技术权重仅 10%，Big Five 90%。
- **失效边界**：绝不做人格判定，输出话术永远是"TA 的行为更像 P 型偏好"而非"TA 是 INFP"。

### M23 Holland 职业兴趣理论（RIASEC）
- **来源与权威性**：John Holland 1959，职业心理学标准模型，美国 O*NET 官方采用。
- **工程化用法**：职业画像六维（现实/研究/艺术/社会/企业/常规）——决定技能市场个性化推荐的主轴：研究型用户多推分析类技能，企业型多推商务文书类。也给"分身资产"提供能力光谱描述。
- **失效边界**：职业迁移期画像滞后——随记忆滚动更新，季度重算。

### M24 领导/客户沟通画像（三模型收敛，自研框架）
- **工程化用法**：对每个高频联系人：Big Five 行为证据（消息里的尽责性线索）+ MBTI 沟通速写 + 关系情境（组织层级/亲密度）→ 输出三件套：偏好文风（直接/委婉）、决策风格（数据流/直觉流）、雷区清单（如"别在周一早找 TA"）。服务于向上管理与客户应对——写出来的东西"像懂 TA 的人写的"。
- **验证**：回测历史消息的响应率/响应速度，画像调整后响应指标应提升。

### M25 SWOT
- **来源与权威性**：1960s Stanford 研究所 Albert Humphrey 团队，战略规划第一课。
- **工程化用法**：企业画像的内部维度（优势/劣势）——从内网资料、用户产出文档中提取该组织的真实能力面，写方案时自动匹配"扬长避短"论据。

### M26 Porter 五力模型
- **来源与权威性**：Michael Porter（哈佛商学院）1979 HBR，竞争战略学科奠基。
- **工程化用法**：企业画像的竞争维度——五力框架化整理用户所在行业的供应商/客户/替代品/新进入者/现有对手信息，写行业分析、竞品报告时直接调用结构化素材。
- **失效边界**：五力是静态框架——只做素材组织，不做趋势预测（趋势交 PESTEL 时间维度）。

## 3.7 战略与商业类（4 个）——管"生意怎么设计"

### M27 PESTEL
- **来源与权威性**：战略分析标准工具（Political/Economic/Social/Tech/Environmental/Legal），Francis Aguilar 1967 ETP 起源，MBA 必修。
- **工程化用法**：企业画像的宏观维度+PAI-Station 自身的合规雷达——把"数据出境、生成内容标识（网信办 AIGC 管理办法）、个人信息保护法 PIPL"等法律变量纳入产品设计与企业画像，技能市场审核规则直接引用 PESTEL-L 层。

### M28 商业模式画布
- **来源与权威性**：Alexander Osterwalder《商业模式新生代》(2010)，全球创业标准工具。
- **工程化用法**：①为企业画像补"价值主张/客户关系/收入来源"三格（用户写商业文档最高频引用的素材）；②PAI-Station 自己的商业模式也用画布推演（第 7 章），保证商业计划各要素完备。

### M29 Kano 模型 + RICE（组合使用）
- **来源与权威性**：Kano（东京理工大学，1984 狩野纪昭）需求分类；RICE（Intercom 团队，硅谷新产品优先级标准：Reach×Impact×Confidence/Effort）。
- **工程化用法**：①需求排序：所有功能按 Kano 分类（基本型/期望型/兴奋型）——本地隐私是基本型（没有即死）、主动服务是期望型、分身资产交易是兴奋型；RICE 定研发顺序（第 8 章排期即 RICE 输出）；②商业上技能市场退款判据：买家按技能描述与实际效果的 Kano 期望差申请退款，平台仲裁用初稿采纳率数据说话。
- **失效边界**：RICE 的 Confidence 主观——用调研数据锚定（350 项调研给了我们罕见的数据密度）。

## 3.8 组合总图谱（模型协同的最终形态）

```
                     ┌── Maslow（价值分层）
愿景 ──JTBD──▶ 任务 ─┼── Fogg B=MAP（时机）── Eisenhower（分流）──▶ 主动交付
                     └── Kano（需求归类）
记忆 ── DIKW──▶ Zettelkasten（组织）── Ebbinghaus（衰减）──▶ 复利记忆
方法 ── Kolb ──▶ Bloom（分级）── Ericsson（反馈）──▶ 技能资产
纠错 ── PDCA ──▶ 5Whys+石川（根因）── 双环（改规则）──▶ 自学习
画像 ── BigFive+MBTI+Holland（人）── SWOT+五力+PESTEL+画布（企业）──▶ 懂你懂你的战场
输出 ── 金字塔+SCQA+MECE+GB/T9704 ──▶ 像你写的东西
调度 ── Kahneman 双系统 + OODA ──▶ 全局节拍与快慢路由
```
29 个模型各就各位，没有一个是为了"显得有文化"而存在的。


---


# 第 4 章 系统架构详设（工程师照此实现）

## 4.1 进程与线程总拓扑

```
┌──────────────────────────────────────────────────────────────┐
│ PAI-Station.exe（pywebview 桌面宿主 + pystray 托盘，用户唯一可见窗口）│
│  ├─ 主循环 scheduler_loop（APScheduler）：20 分钟节拍 OODA      │
│  ├─ 感知线程：watchdog 事件 / mss 截图 / 前台窗口轮询            │
│  ├─ 通道线程×N：lark-oapi WS / dingtalk-stream / 企微 WS       │
│  ├─ 视觉线程：截图→GLM-4V-Flash→结构化事件（断网→PaddleOCR）     │
│  └─ LLM 适配器：pai/llm/（fast/deep/vision 三通道）             │
├──────────────────────────── IPC（localhost HTTP/stdio）────────┤
│ openviking-sidecar.exe（AGPL 隔离）：viking:// 分层记忆检索      │
├──────────────────────────────────────────────────────────────┤
│ WinSW 包装为 Windows 服务：开机自启、崩溃自动重启、7×24          │
└──────────────────────────────────────────────────────────────┘
数据面：memory.db / rules/ / skills/ / outbox.db / audit.db（全部 %LOCALAPPDATA%\PAI-Station\）
```

关键设计决定：
1. **单主进程多线程**（不是微服务）——个人电脑单用户场景，进程间通信只用于 AGPL 隔离这一处硬需求；
2. **Sidecar 模式隔离 OpenViking**——进程边界阻断许可证传染，协议走本地 HTTP（127.0.0.1:17777）；
3. **WinSW 守护**——Windows 服务化后断电重启自动拉起，满足 7×24 要求；
4. **断网降级链**——云端 glm-4v-flash 不可用时截图转本地 PaddleOCR，云端 chat 不可用时任务入队延迟执行，感知与存储永不中断。

## 4.2 代码目录结构（src/paistation/）

```
src/paistation/
├── main.py               # 入口：装配各引擎、启动调度器
├── config.py             # INI 加载与校验（zod 式校验，错误友好提示）
├── llm/                  # 智谱免费链适配器：fast()/deep()/vision()
│   ├── zhipu_client.py   #   _AdaptivePacer 限速 + tenacity 重试（移植 We-AIPO）
│   └── router.py         #   Kahneman 双系统路由（置信度阈值升级）
├── sense/                # L1 感知层
│   ├── fs_watcher.py     #   watchdog 文件监听（去抖+批量）
│   ├── screen.py         #   mss 截图 + 黑名单窗口过滤 + 10 分钟滚动缓冲
│   ├── vision.py         #   截图→GLM-4V-Flash→结构化事件 JSON
│   ├── clipboard.py      #   剪贴板监听（脱敏：密码形状内容直接丢弃）
│   └── fgwindow.py       #   前台窗口轮询（注意力状态判定）
├── memory/               # L2 上下文层
│   ├── ingest.py         #   DIKW 流水线：抽取→摘要→打分→落库
│   ├── viking_client.py  #   sidecar IPC 客户端
│   ├── store.py          #   SQLite FTS5 + jieba
│   └── recall.py         #   三路召回：关键词+实体图+时间线
├── rules/                # L3 规则层
│   ├── personal.py       #   个人规则蒸馏与冲突检测
│   ├── contacts.py       #   人际画像三件套
│   └── org.py            #   企业画像四框架（SWOT/五力/PESTEL/画布）
├── skills/               # L4 技能层
│   ├── executor.py       #   SKILL.md 加载→上下文组装→执行
│   ├── distiller.py      #   初终稿 diff 蒸馏器（核心自研）
│   ├── scanner.py        #   技能安全扫描（移植 QwenPaw 五层范式）
│   └── market.py         #   技能市场发布/安装/更新（GitHub 零服务器）
├── proactive/            # L5 主动层
│   ├── triggers.py       #   事件触发器（日历/文件/IM/周期）
│   ├── timing.py         #   Fogg B=MAP 打分 + 注意力状态
│   ├── quadrant.py       #   Eisenhower 分流
│   └── outbox.py         #   成果抽屉 + 企微推送
├── learn/                # L6 自学习层
│   ├── pdca.py           #   每日复盘循环
│   ├── whys.py           #   5Whys 根因（z1 推理链+日志证据）
│   └── double_loop.py    #   双环学习（重大变更走用户审批）
├── channels/             # 通道层
│   ├── feishu.py / dingtalk.py / wecom.py / wechat_vision.py
├── security/             # 安全层
│   ├── file_guard.py     #   白名单目录（默认仅 Documents/Desktop/Downloads）
│   ├── tool_guard.py     #   工具调用审批（危险命令需确认）
│   └── audit.py          #   全动作审计日志（audit.db）
└── ui/
    ├── tray.py           #   托盘：抽屉入口/暂停感知/设置
    └── drawer.py         #   抽屉 UI：成果/记忆星图/技能库/画像
```

每个文件 200-400 行（遵循小文件原则），全项目预计 8,000-12,000 行，其中自研四处（distiller/timing/画像/安全）约 2,500 行。

## 4.3 模块间接口契约（关键 5 个）

```
① LLM 适配器（全项目唯一模型入口）
   fast(prompt, ctx=128K) -> {text, confidence, usage}
   deep(prompt, reasoning=True) -> {text, chain, confidence}
   vision(image_path, schema) -> {json}        # schema 约束输出结构

② 记忆写入 ingest.capture(event) -> memory_id
   event = {type, source_path, content, ts}
   处理：抽取→摘要→实体→打分→写库→（若高价值）同步 viking sidecar

③ 记忆召回 recall.query(nl_question, top_k=8) -> [{mem, score, path}]
   三路召回→RRF 融合→返回带出处

④ 主动评分 timing.evaluate(trigger_event) -> {B, quadrant, action}
   action ∈ {push_wecom, drawer, folded_reminder, drop}

⑤ 技能执行 executor.run(skill_id, task) -> {draft_path, usage}
   初稿落盘后监听用户编辑，diff 回写 distiller.feedback()
```

## 4.4 数据库 Schema（四库，全部 SQLite）

```sql
-- memory.db：记忆库（P1）
CREATE TABLE memory (
  id TEXT PRIMARY KEY,          -- UUID
  ts INTEGER NOT NULL,          -- 事件时间
  created_at INTEGER,           -- 入库时间
  type TEXT NOT NULL,           -- doc/screen/im/commit/calendar/browse
  source_path TEXT,             -- 出处（可溯源）
  source_hash TEXT,             -- 内容指纹（去重）
  summary TEXT NOT NULL,        -- ≤200 字事实摘要（L1）
  entities TEXT,                -- JSON 数组：人/项目/客户/日期
  tags TEXT,                    -- 主题标签
  weight REAL DEFAULT 1.0,      -- Ebbinghaus 权重
  links TEXT                    -- Zettelkasten 互链 id 数组
);
CREATE VIRTUAL TABLE memory_fts USING fts5(summary, entities, tags, content=memory);

-- outbox.db：主动成果（P3）
CREATE TABLE outbox (
  id TEXT PRIMARY KEY, ts INTEGER, trigger TEXT,   -- 触发事件
  content_path TEXT, score REAL, quadrant INTEGER, -- B 分与象限
  action TEXT, status TEXT,                        -- pending/adopted/rejected/expired
  adopted_diff REAL                                -- 采纳率（编辑距离比）
);

-- skills.db：技能注册表（P2）
CREATE TABLE skills (
  id TEXT PRIMARY KEY, name TEXT, version TEXT,
  level INTEGER,          -- Bloom 层级
  adopt_rate REAL,        -- 滚动采纳率
  sample_count INTEGER,   -- 蒸馏样本数（<5 标"学习中"）
  origin TEXT,            -- distilled/market/builtin
  updated_at INTEGER
);

-- audit.db：审计日志（安全层）
CREATE TABLE audit (
  id INTEGER PRIMARY KEY, ts INTEGER, actor TEXT,   -- engine/skill/user
  action TEXT, target TEXT, detail TEXT, result TEXT
);
```

规则与技能本体是文件不是表：`rules/*.ini|json`（用户可直接编辑）与 `skills/<id>/SKILL.md`（git 化）。

## 4.5 INI 配置全集（用户可改的一切，遵循"用户配置走 INI"约定）

```ini
;; config/pai.ini —— 主配置（安装时生成，全部有注释与默认值）
[llm]
api_key = <智谱 key，可被环境变量 PAI_LLM_KEY 覆盖>
fast_model = glm-4-flash
deep_model = glm-z1-flash
vision_model = glm-4v-flash
long_model = glm-4.7-flash
upgrade_confidence = 0.7        ; 系统1→系统2 升级阈值

[sense]
watch_dirs = ~/Documents, ~/Desktop, ~/Downloads   ; 白名单，逗号分隔
screen_interval_sec = 600        ; 截图节拍（10 分钟）
screen_blacklist = 密码管理器, 银行, 支付            ; 窗口标题关键词黑名单
fg_poll_sec = 300               ; 前台窗口轮询
deepwork_apps = WINWORD.EXE, idea64.exe, Code.exe    ; 深工作判定（P=0 不打扰）

[channels]
feishu_enabled = 1 ; dingtalk_enabled = 0 ; wecom_enabled = 1
wechat_mode = vision_readonly    ; 微信只允许纯视觉只读，硬编码不可改
notify_webhook = https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=...

[proactive]
max_push_per_day = 2             ; 打扰预算
drawer_retention_days = 7
quiet_hours = 22:00-07:00        ; 静默时段（仅抽屉）

[privacy]
data_dir = %LOCALAPPDATA%\PAI-Station
export_on_exit = 0
never_send = 原始截图, 凭据类文件 ; 永不上云的内容类型

[learn]
pdca_time = 02:00                ; 每日复盘
distill_min_samples = 5          ; 蒸馏最小样本
double_loop_confirm = 1          ; 双环改动需用户确认
```

读取约定：`config.py` 启动时全量校验（类型/范围/路径存在），错误用中文报出"哪个键、期望什么、实际什么"，绝不带病运行。

## 4.6 关键数据流走查（三条端到端用例）

**用例 A：文件感知→记忆**
新合同落 Desktop → watchdog 事件（去抖 3 秒合并）→ MarkItDown 转 MD（失败则 MinerU）→ ingest.capture() → glm-4-flash 摘要+实体（glm-4.7-flash 若 >50 页）→ 打分（合同类 source 高权重）→ 写 memory.db + FTS5 索引 → 实体链接（同供应商旧记忆互链）。全程 ≤90 秒，audit.db 记录每步。

**用例 B：主动服务→抽屉**
周五 15:40（周期触发器）→ timing.evaluate：M=5×4×3=60，A=技能库有"周报"技能（sample≥5, adopt 0.62）→A 高；P：前台是浏览器=可用 → B=高 → Eisenhower：重要不紧急 → 抽屉。executor.run 生成初稿 docx → outbox 状态 pending → 托盘图标轻闪（非弹窗）。用户 16:00 打开抽屉→采纳/编辑→diff 回写→adopted。

**用例 C：IM 命令→执行**
飞书消息"把上季度供应商合同账期整理成表"→ lark-oapi WS 回调 → 权限校验（仅白名单用户可指挥）→ recall.query("供应商 合同 账期") → 8 条记忆 → executor 用表格技能生成 xlsx → 回传飞书文件。全链路 <60 秒，audit 全留痕。

## 4.7 安全架构（五层，QwenPaw 范式中国化）

| 层 | 机制 | 中国化/强化点 |
|---|---|---|
| 1 沙箱 | 技能脚本在受限解释器跑（禁 import os 直接写盘） | 白名单目录外全拒 |
| 2 Tool Guard | 危险命令（删/网/发）二次确认 | 企微审批卡片确认 |
| 3 File Guard | 路径白名单+黑名单双控 | 微信进程内存零接触 |
| 4 Skill Scanner | 市场技能静态扫描 | 硬编码密钥/反连/提权命令一票否决 |
| 5 Access Policy | IM 通道指令仅限白名单用户 | 通道分级：飞书全指令/微信只读 |

铁律：**原始截图与凭据类内容永不上云**（vision 调用前先黑名单过滤+敏感区域打码重截图）；数据默认不出本机，云调用仅发送脱敏摘要文本。

## 4.8 性能与资源预算（笔记本约束下）

| 指标 | 预算 | 达标手段 |
|---|---|---|
| 内存常驻 | ≤400MB（不含 sidecar） | 截图缓冲滚动清理；FTS5 单文件；线程池固定 8 |
| CPU 空闲 | ≤2% | 事件驱动+20 分钟节拍，无高频轮询 |
| 磁盘增长 | ≤5GB/年 | Ebbinghaus 降级+原文不长期保留 |
| LLM 成本 | ¥0 | 智谱免费链（30 并发/128K 足够单用户） |
| 单任务端到端 | P90 ≤5 分钟 | 队列并行+fast 通道优先 |

## 4.9 与 19 个缝合件的集成验收（每件一条硬标准）

1. OpenViking：LoCoMo 子集召回 ≥75%；2. MarkItDown：Office 三件套转 MD 成功率 ≥95%；3. MinerU：扫描 PDF 版面还原抽检合格；4. mss：4K 截图 <100ms；5. GLM-4V-Flash：窗口识别准确率 ≥90%（自建 200 张标注集）；6. PaddleOCR：断网兜底 CER ≤10%；7. DrissionPage：内网登录态保持 ≥24h；8. lark-oapi：WS 断线重连 ≤30s；9. dingtalk-stream：同左；10. 企微：主动推送到达 ≤5s；11. SenseVoice：会议转写 CER ≤8%；12. APScheduler：错过节拍补跑；13. watchdog：万级文件目录无风暴；14. tenacity：智谱 429 退避后成功率 ≥99%；15. python-docx：GB/T 9704 版式抽检通过；16. SQLite：10 万记忆查询 <300ms；17. pywebview/pystray：Win10 崩溃零；18. WinSW：断电重启自愈；19. 智谱链：三通道切换零代码改动。


---


# 第 5 章 中国 IM 通道实施手册（逐平台操作级）

> 目标：把 openhuman 的"17 通道全是西式应用"改造成中国职场四大 IM 全覆盖。每个通道给出：接入方式选型对比、注册配置步骤（照做即可）、代码骨架、风险控制、验收标准。

## 5.1 四通道总览

| 通道 | 接入方式 | 公网暴露 | 能力 | 定位 |
|---|---|---|---|---|
| 飞书 | lark-oapi WS 长连接 | 不需要 | 收发消息/文件/卡片/日程 | 全功能主通道（先做） |
| 钉钉 | dingtalk-stream | 不需要 | 收发消息/文件 | 全功能 |
| 企业微信 | 自建应用（REST+回调） | 需要（或内网穿透） | 收发消息/审批卡片 | 主通道+审批确认入口 |
| 微信 | 纯视觉只读 | 不需要 | 只读感知（截图→VLM） | 感知通道，绝不自动化 |

## 5.2 飞书通道（lark-oapi，WebSocket 模式）

**为什么选 WS 模式**：飞书开放平台提供事件订阅两种模式——Webhook 回调（需要公网 IP/域名/备案）与长连接（本机直连，无需任何公网暴露）。个人笔记本场景 WS 是唯一顺滑解。

**配置步骤（照做）**：
1. open.feishu.cn → 创建企业自建应用（个人可注册免费版飞书组织）；
2. 权限管理开通：`im:message`、`im:message.group_at_msg`、`im:resource`（下载消息中的文件）；
3. 事件与回调 → 订阅方式选"使用长连接接收事件"→ 订阅 `im.message.receive_v1`；
4. 凭证与基础信息 → 拿到 App ID / App Secret 填入 `config/feishu.ini`；
5. 版本管理与发布 → 应用上线（自建应用免审）；
6. 把 PAI-Station 机器人拉进需要的群或加为单聊联系人。

**代码骨架**：
```python
import lark_oapi as lark
def on_message(ctx, conf, event):        # im.message.receive_v1
    text = extract_text(event.message)   # 富文本/图片消息分别处理
    task = classify(text)                # glm-4-flash 系统1分类
    enqueue(task, reply_to=event.message.chat_id)
client = lark.ws.Client(APP_ID, APP_SECRET,
                        event_handler=dispatcher, log_level=lark.LogLevel.INFO)
client.start()                            # 阻塞线程，断线自动重连
```
**验收**：群里 @ 机器人"整理今日会议要点"→ 60 秒内回卡片，附出处文件。

## 5.3 钉钉通道（dingtalk-stream）

**选型**：钉钉 Stream 模式同样免公网（对比 HTTP 回调模式）。开放平台 create.oa.dingtalk.com → 应用开发 → 消息推送选 Stream 模式 → 拿 Client ID/Secret → 订阅企业内部机器人消息事件。代码骨架与飞书同构（两个通道共用 `channels/base.py` 抽象：on_message/on_confirm/send_file）。
**验收**：单聊发文档→收到摘要回复；断网 30 秒恢复后消息不丢（Stream 补投）。

## 5.4 企业微信通道（自建应用）

**步骤**：
1. work.weixin.qq.com 注册企业（个人可注册，无需对公）→ 应用管理 → 创建自建应用"PAI-Station"；
2. 拿 CorpID + Secret + AgentID 填 `config/wecom.ini`；
3. 接收消息：两方案——A. 回调模式（需公网，用 cpolar/natapp 内网穿透或家宽公网）；B. **轮询模式（默认）**：定时拉取通讯录同步与日程变化，机器人主动发消息（webhook 或应用消息 API），用户指令经"客户消息"接口被动回复。默认 B 起步（零公网），进阶用户开 A；
4. 主动推送：应用消息 API `message/send`（markdown 卡片）——这就是主动服务的推送通道，也是双环学习审批的确认入口。

**审批卡片（创新点）**：双环学习的重大变更、Tool Guard 危险命令确认，都发企微 markdown 卡片给用户，回复"同意/拒绝"完成闭环——把审批搬到用户最常看的地方。
**验收**：主动推送 P90 到达 ≤5 秒；审批回复在 24h 内回收率 ≥80%。

## 5.5 微信通道（纯视觉只读——本项目最重要的安全决策）

**背景**：微信没有个人可用的开放接口。历史方案全线阵亡：网页版微信（2017 年起大规模封禁）、ItChat/itchat（同源，失效）、协议逆向（强风险）、Xposed/Hook（root/越狱+封号）。**个人微信自动化在微信官方明确禁止范围**——任何 Hook 方案对用户都是封号赌博，作为产品不可接受。

**我们的方案：纯视觉只读**
```
微信窗口（PC 版）→ mss 定时/事件截图 → 黑名单过滤 → GLM-4V-Flash 结构化识别
→ {发送人/内容摘要/时间/情绪信号} → 作为记忆事件入 memory.db
→ 触发器：识别到"领导@你 要材料"→ 主动层评估 → 企微/飞书推送提醒
```
四条铁律（写死在代码，INI 改不了）：
1. **只截图，零注入**——不 Hook、不模拟点击、不发消息、不加好友；
2. **只读公开界面**——密码框/支付页黑名单整窗丢弃；
3. **原文不存**——只存 VLM 产出的摘要与实体，原始截图 10 分钟滚动销毁；
4. **默认关闭**——首次开启弹窗明示风险与用途，用户主动确认才启用。

**验收**：连续挂机 30 天微信零异常（无风控提示、无强制下线）；识别准确率抽检 ≥85%；原始截图文件系统断言：10 分钟后缓冲目录为空。

## 5.6 通道抽象层（channels/base.py）

```python
class Channel(ABC):
    @abstractmethod
    def send_text(self, to, text): ...
    @abstractmethod
    def send_file(self, to, path): ...
    @abstractmethod
    def send_confirm(self, to, question, options) -> confirm_id: ...
    def listen(self): ...                      # 各自实现 WS/轮询/视觉
    def register_router(self, fn): ...         # 指令路由：classify→技能
```
新通道（QQ/Telegram）只需继承 Channel 实现 4 个方法——为社区贡献通道留好缝。


---


# 第 6 章 生态战略详论（三层飞轮动力学与竞品生态能力论证）

> 用户指令：融入生态思维；售卖这个产品玩的就是生态；三层飞轮要写明怎么转动起来；为什么竞品做不了生态要充分调研（GitHub+网络+产品官网）给出详尽理由。本章逐层回答：冷启动怎么做、正反馈回路是什么、转动指标是多少、卡点在哪。

## 6.1 三层飞轮总模型

```
   第一层 组件生态（借势）                第二层 技能生态（自增殖）           第三层 用户生态（资产网络）
   PAI 适配并反哺 350 项调研件    ──▶    用户蒸馏技能上架市场        ──▶    分身资产沉淀形成数据护城河
   ▲                                    ▲    │买家付费→抽成→作者激励        │
   │ 新组件带来新能力→吸引开发者          │    └技能越多→新用户越多→蒸馏更多  └资产越厚→迁移成本越高→留存越深
   └──────────────────────────────────────┴───────────────────────────────────────┘
```
三层不是串联而是咬合：组件层供弹药（技能要调工具），技能层聚人气，用户层变现与筑壁垒。**软件本身免费**——收入全部来自生态（第 7 章）。

## 6.2 第一层：组件生态怎么转起来

**冷启动（M0-M1，第 1-2 个月）**：
1. 把 350 项调研中的 19 项缝合件做成"适配器+文档"，每个适配器一个独立 PR/README，被上游项目引用（给 OpenViking 提 sidecar 集成示例 PR、给 DrissionPage 写办公自动化案例）；
2. 发布《个人 AI 工作站开源组件选型报告》（350 项对比表本身就是内容资产）——开发者搜索"watchdog 截图""lark-oapi 教程"时流量自然进来；
3. 给上游项目提 issue/PR 修 bug（真实使用中发现的问题），建立技术信誉。

**正反馈回路**：新组件适配 → 新技能能力 → 用户增长 → 用户暴露新需求 → 社区提交新组件适配 → 循环。
**转动指标**：适配器数量（首年目标 30 个）、上游互链（被 openviking/drissionpage README 引用）、社区 PR 数（月均 ≥5）。
**卡点与对策**：AGPL 许可证组件（OpenViking）用 sidecar 隔离，并公开隔离方案给社区复用——把合规难点变成技术博客素材。

## 6.3 第二层：技能生态怎么转起来（飞轮的心脏）

**双边市场设计**：
- 供给侧：每个重度用户工作满 3 个月自动积累 10-50 个蒸馏技能（P2 引擎自动产出，零额外劳动）；上架走 GitHub 仓库模式——技能=Git repo（SKILL.md+资源），市场只做**索引与结算，不托管文件**（零服务器成本，天然抗审查，与 anthropics/skills 生态互通）；
- 需求侧：新用户装完 PAI 第一件事逛技能市场——"资深造价工程师技能包""红头文件技能包""跨境电商跟单技能包"装上即用（技能=别人的方法论）。

**冷启动（M3-M4，第 4-6 个月）**：
1. 我们团队+种子用户预蒸馏 100 个高频职业技能（周报/纪要/公文/简历/合同审查/数据报表六大家族）；
2. 每个技能附"采纳率"公开标签（技能市场独有：数据化信任）；
3. 种子作者计划：前 100 名上架者免佣金一年+流量扶持。

**正反馈回路**：技能越多→新用户越容易上手→用户越多→蒸馏越多→技能越多（经典双边网络效应，K 因子 = 每个用户平均带来技能数 × 下载转化）。
**转动指标**：技能总数（首年 1,000）、月活跃技能数、作者数（首年 200）、供需比（浏览/上架 <50 为健康）。
**结算机制**：技能定价 0-99 元；买家付费走微信/支付宝（M5 接入，前 500 单人工对账起步）；平台抽成 15%。

## 6.4 第三层：用户生态怎么转起来

**冷启动**：用户建分身资产（P4）满 90 天 → "分身年度报告"（你的分身替你工作了 X 小时、掌握 Y 项技能）——可分享海报，社交裂变入口。
**正反馈回路**：资产越厚→分身越好用→越离不开→持续订阅增值服务（托管备份/多端同步/高级技能市场特权）→付费支持产品迭代→更好用。
**转动指标**：90 天留存 ≥60%、资产包导出率（健康信号：用户在管理资产而非流失）、NPS ≥40。

## 6.5 竞品为什么做不了生态（逐项证据调研）

> 调研来源：各产品 GitHub README/官网/文档（2026-09-06 实地抓取），逐家论证结构性障碍，不是一笔带过。

### ① openhuman（GitHub 39,436★；官网 tinyhumans.ai/openhuman）
官网实证（本次抓取原文）：
- "Connect up to **100 apps, 10,000 MCPs, 90,000 Skills**"——集成生态极其繁荣，但逐项看官网展示的 app 列表：**Notion、Gmail、Telegram、Slack、Sheets、Excel、Teams、Github**——全部是西式生产力应用，零中国 IM；
- 许可证 **GPL-3**（官网 Grok 推文亦确认）——传染性许可证，商业生态参与者（企业开发者）贡献即受 GPL 约束，技能市场的商业化改造（闭源结算/抽成系统）与主程序 GPL 冲突，**商业生态天然瘸腿**；
- 商业模式：订阅制托管服务（官网 Try Out + 订阅），订阅制意味着平台与用户是"租约关系"——用户数据在平台侧的记忆图（官网 layer-four 图：7,349 节点 built from connected apps）——**分身资产不在用户手里，用户生态（资产层）无从谈起**；
- 其 agent 经济（tiny.place/x402 微支付）面向 agent-to-agent 交易，不是个人技能市场。
**结论**：openhuman 做的是"接入生态"（Western SaaS connector 矩阵），不是"价值生态"（技能生产-交易-复利）。结构性原因：①GPL 阻断商业闭环；②订阅制使资产留在平台；③中国通道空白使 90% 中国职场工作流进不来。

### ② QwenPaw（GitHub 34,919★；官网 qwenpaw.agentscope.io）
官网实证（本次抓取 meta 描述原文）：
- "connects to 10+ channels including **DingTalk, Feishu, QQ, Discord, and Telegram**…extensible with **Skills and MCP**"——通道中国化做得最好，也支持技能与 MCP 扩展——**这是四竞品中最接近生态的一家**，必须严肃对待；
- 但逐层拆解：①技能是**人工编写/手动安装**（官方技能库+社区提交），无自动蒸馏——技能供给靠开发者爱心，边际成本不趋零；②无全盘感知（屏幕/文件/IM 行为不入上下文）——分身资产无原料；③桌面端仅 Beta，主形态是 IM 对话机器人——"记住你的一切"这个资产前提不成立；④归属阿里巴巴生态（Qwen 系），其战略重心是 Qwen 模型云服务与 AgentScope 框架标准，个人工作站是框架的展示用例——**资源不会长期压在单机个人市场**（大厂创新者窘境：单机免费产品无收入回路面）。
**结论**：QwenPaw 有通道与技能的"生态形式"，缺"生态引擎"（自动蒸馏供给+分身资产沉淀）。我们 vs QwenPaw 的胜负手：蒸馏器与资产层——恰好是 P2/P4 两支柱。

### ③ OpenViking（GitHub 35,602★；volcengine 火山引擎出品）
- 定位：**记忆中间件/组件**（viking:// 虚拟文件系统，VLDB 2026 VikingMem 论文），不是终端产品——它自己就不做用户生态，做的是"被生态集成"（我们是它的集成者）；
- 许可证 AGPLv3-core：任何直接链接它做终端产品的竞品同样面临传染问题——**我们已用 sidecar 隔离方案解决，此方案竞品尚未公开采用**；
- 火山引擎的商业重心是云服务（豆包大模型云），OpenViking 是技术影响力投资。
**结论**：组件不做生态——正因如此，它成为我们组件层最优质的缝合件（借势不竞争）。

### ④ nanobot（GitHub 47,726★；HKUDS 出品）
- 学术团队出品，卖点是 ultra-light 极简循环与社区通道；GitHub 描述与 README 显示其哲学是"小而美、社区驱动"——**没有商业化组织做市场运营**（技能市场需要结算、审核、客服、合规——学术项目无力长期运营）；
- 无中国 IM 官方通道（社区方案为主），无感知、无蒸馏、无画像。
**结论**：能赢 GitHub 星标，赢不了需要持续运营的双边市场。

### ⑤ OpenClaw（参考基准）
- 个人助理开源标杆，但通道以 Telegram/Discord 为主，无技能市场，无中国化；其记忆方案在 LoCoMo 基准 24.2%（OpenViking 论文实测）——上下文能力是其公认短板，而上下文恰是生态的地基。

### 综合结论：生态能力五维打分表（证据见上）

| 生态能力维度 | openhuman | QwenPaw | OpenViking | nanobot | **PAI-Station** |
|---|---|---|---|---|---|
| 中国 IM 原生通道 | 0/10 | 8/10 | - | 2/10 | **10/10** |
| 技能自动供给（蒸馏） | 0/10 | 0/10 | - | 0/10 | **9/10**（自研 diff 蒸馏） |
| 技能双边市场（结算/抽成/审核） | 3/10（agent经济，非个人技能市场） | 4/10（技能库无结算） | - | 2/10 | **8/10**（M5 上线） |
| 用户资产归属（本地分身） | 3/10（订阅+平台侧记忆） | 4/10 | - | 4/10 | **10/10**（本地资产包） |
| 零成本运行（免费模型链） | 0/10（绑定付费 API） | 5/10（本地模型可但质量差） | - | 5/10 | **10/10**（智谱免费链） |
| **合计（25 分制）** | 6 | 21 | - | 13 | **47/50** |

**一句话论证**：生态=形式（通道/技能框架）+引擎（自动供给+资产沉淀+商业结算）。竞品最多有形式无引擎；引擎三件套（蒸馏器/资产包/市场结算）正是我们 P2/P4 与第 7 章的设计主体——**这不是功能差异，是商业模式的结构差异**，竞品转向需推翻自身订阅制/许可证/组织战略，结构性不可追随。


---


# 第 7 章 商业计划书详解（从愿景到收入的每一步推导）

> 用户指令：商业计划每步怎么实现、怎么得出的（愿景→战略→实施计划→技术调研→技术途径→一步步）。本章的每个数字都给出推导链条，不接受"拍脑袋"数字。

## 7.1 商业逻辑总推导链

```
愿景（个人 AI 工作站普及）
 └─▶ 战略：软件免费获客 × 生态变现（技能市场为主引擎）
      ├─ 为什么免费：单机软件付费天花板低（盗版+竞品开源），且免费是获客飞轮燃料
      ├─ 为什么生态：用户资产（技能/画像）随时间增值→付费意愿来自增值服务与交易而非许可费
      └─ 四收入引擎 RICE 排序（RICE=Reach×Impact×Confidence/Effort）：
          技能市场 9.6 ＞ Token 计量增值 6.3 ＞ 企业版 2.3 ＞ 数据增值服务 1.0
```

**RICE 评分明细（怎么得出的）**：
| 引擎 | Reach(人/年) | Impact(1-3) | Confidence(0-1) | Effort(人月) | RICE | 依据 |
|---|---|---|---|---|---|---|
| 技能市场 | 400,000 DAU×5%购买 | 3（客单价抽成+双边飞轮） | 0.8（App Store/GPT Store 先例） | 10 | **9.6** | 苹果 App Store 抽成 15-30% 模型验证 |
| Token 增值 | 400,000×10% | 2 | 0.7（智谱额度数据） | 4 | 6.3 | 免费链月赠 ~3000万 token，重度用户超限 |
| 企业版 | 5,000 企业 | 3 | 0.5 | 20 | 2.3 | 私有化部署+合规需求 |
| 数据服务 | 100,000 | 1 | 0.3（合规敏感） | 8 | 1.0 | 匿名化行业技能需求报告 |

## 7.2 引擎一：技能市场（第一杠杆，逐项实施步骤）

**第 1 步 市场机制（M4-M5，2 个月）**
- 技能=Git 仓库；市场=官方索引仓库（pai-station/market-index）+ 网页目录站（静态生成，零服务器）；
- 支付：首期人工对账（用户扫码付给作者+平台收款码双码并行，500 单内可行）→ 验证需求后接微信支付商户（M6+）；
- 审核：Skill Scanner 自动扫（密钥/后门/侵权关键词）+ 人工抽检双闸。
**第 2 步 供给运营（持续）**
- 种子 100 技能自产（六大家族：周报/纪要/公文/简历/合同/报表，各 ~15 个变体）；
- 作者激励：前 100 名免佣一年；月度采纳率 TOP10 现金奖励（首年预算 2 万/月）；
- 定价锚：参照 GPT Store/豆包插件免费混乱的反面——**付费技能的信任标签就是采纳率数据**（我们独有）。
**第 3 步 抽成模型（怎么得出 15%）**
- App Store 30% 抽成因垄断渠道；Google Play 15%；国内安卓渠道 50%；Gumroad 10%；
- 我们是"索引+结算"轻职责（不托管、不分发流量为主），对标 Gumroad+Google Play 之间 → **15%**：作者得 85%——高分成吸引供给，市场早期让利换生态；
- 测算：客单价 ¥19 × 400 万 DAU × 5% 月购买率 × 15% 抽成 ≈ **¥570 万/月**（成熟期）。
**第 4 步 反作弊**：刷单检测（同设备/支付指纹/采纳率-销量背离），封禁清单公开。

## 7.3 引擎二：Token 计量增值（第二杠杆）

**事实基础**：智谱免费链 glm-4-flash 并发 30、月赠 token 额度约 3,000 万级——轻度用户（日均 <100 万 token）完全够用；重度用户（全盘感知+高频蒸馏）月耗 1-3 亿 token 超限。
**实施**：本地 Token 计量表（usage 字段已在 Schema）→ 超免费额度后引导：①降级（感知频率×0.5，免费）；②升级 Pro ¥19/月（平台代付智谱 GLM-4-Plus 等价通道+更快调度）。**伦理设计**：免费档永不残废——核心功能降频不阉割（Kano 基本型需求免费保底）。
**测算**：400 万 DAU × 10% 付费 × ¥19 × 12 ≈ **¥9,120 万/年**（成熟期）。

## 7.4 引擎三：企业版与引擎四：数据增值

**企业版（M8+）**：私有化部署（智谱企业通道/本地模型）、组织技能库（团队共享蒸馏技能）、合规审计包（PIPL/数据出境）。定价 ¥39/席位/月 起，1,000 席中型企业年费 ~47 万。首年目标 50 家试点 → ~500 万。
**数据增值（远期，谨慎）**：仅做**用户主动授权**的匿名化行业报告（如"互联网行业周报平均耗时报告"），严格遵守 PIPL——Confidence 低，排在最后。

## 7.5 定价阶梯全景（怎么得出的每一档）

| 档 | 价格 | 包含 | 定价依据 |
|---|---|---|---|
| Free | ¥0 | 全功能+免费模型链+抽屉 | 获客飞轮燃料；智谱免费链使边际成本≈0 |
| Pro | ¥19/月 | 更快模型调度+超额度 Token+云备份分身 | 对标一杯奶茶/爱奇艺会员的心理账户；<1% 日薪的"无感价位" |
| Team | ¥39/席位/月 | 组织技能库+管理台 | 对标飞书/钉钉增值席位定价惯例 |
| Enterprise | 议价 | 私有化+合规包 | 项目制报价 |
| 技能市场 | 0-99 元/件 | 单件买断 | Gumroad 数字商品带（<99 元冲动消费区间） |

## 7.6 收入演进推演（保守/基准/乐观三档）

| 时间 | 保守 | 基准 | 乐观 | 假设（基准档） |
|---|---|---|---|---|
| 第 1 年 | ¥0（纯建设） | ¥3 万 | ¥30 万 | DAU 1 万，市场早期，种子用户免佣 |
| 第 2 年 | ¥80 万 | ¥680 万 | ¥2,500 万 | DAU 30 万，Pro 渗透 4%，市场抽成起步 |
| 第 3 年 | ¥800 万 | ¥6,800 万 | ¥2.2 亿 | DAU 150 万，Pro 8%，市场成熟，企业版 50 家 |
| 第 4 年 | ¥3,000 万 | ¥2.1 亿 | ¥7 亿 | DAU 400 万（4 亿×1%），复利生态成型 |
| 第 5 年 | ¥1 亿 | ¥6-8 亿 | ¥20 亿+ | 技能市场网络效应+出海（东南亚中文圈） |

敏感性：收入对 DAU 弹性最大（1.5 次方——双边网络效应），对价格弹性小（-0.3，订阅价位处低敏区）；最大风险变量是获客 CAC（详见第 10 章风险 R3）。

## 7.7 竞争壁垒四件套（钱烧不出来的）

1. **数据护城河**：用户 90 天记忆资产=迁移成本，竞品产品再好也搬不走你的记忆库（格式专有+复利价值）；
2. **双边网络效应**：技能市场作者-买家双边锁定（WeChat 生态先例：先发双边市场几乎不可逆转）；
3. **蒸馏引擎**：竞品补做需要完整感知→diff→蒸馏管线（我们 P1+P2 联调 6 个月+数据积累）；
4. **中国合规经验**：微信纯视觉协议、GB/T 9704、PIPL 本地化——海外开源项目短期无动力跟进。


---


# 第 8 章 实施计划与进度（任务级排期，RICE 排序）

> 排期原则：每个里程碑以"可用验收"收口（不是代码写完，而是第 1.3-1.6 节的六段式验收⑥通过）；单人全栈+AI 辅助开发的节奏（用户现有模式：We-AIPO 已验证 1 人+AI 高产可行性）；依赖关系决定顺序，RICE 决定同层内先后。

## 8.1 里程碑总表

| 里程碑 | 内容 | 时长 | 出口判据（全部可测） |
|---|---|---|---|
| M0 地基 | 骨架+配置+LLM 链+审计 | 2 周 | 三通道切换零代码；INI 校验报错友好；audit 全留痕 |
| M1 感知与记忆 | L1+L2 层 | 4 周 | 用例 A 走查通过；500 事实召回 ≥90%；隐私断言通过 |
| M2 通道 | 飞书+企微+微信只读 | 3 周 | 三通道验收（5.2-5.5）；微信 30 天零异常开始累计 |
| M3 技能与主动 | L4+L5 层 | 5 周 | 蒸馏消融 ≥25pp；每周 ≥3 采纳成果；打扰 ≤2/天 |
| M4 画像与自学习 | L3+L6 层 | 4 周 | 画像证据链可解释；PDCA 每日跑；5Whys 根因分类准确抽检 ≥80% |
| M5 生态发布 | 市场+打包+文档+发布 | 4 周 | 100 种子技能上架；安装包一键装；README 病毒级；GitHub 发布 |
| 合计 | | **22 周 ≈ 5.5 个月** | |

## 8.2 M0 地基（第 1-2 周）任务分解

| # | 任务 | 产出 | 天 |
|---|---|---|---|
| 0.1 | 项目骨架（src/paistation 4.2 结构）+pyproject+lint+CI | 可运行空壳 | 1 |
| 0.2 | config.py INI 加载校验 | pai.ini 全集+中文报错 | 1 |
| 0.3 | llm/zhipu_client.py（移植 We-AIPO zhipu.py：_AdaptivePacer+tenacity） | fast/deep/vision 三方法 | 2 |
| 0.4 | llm/router.py Kahneman 路由（置信度升级） | 单测：升级触发正确 | 1 |
| 0.5 | security/audit.py + file_guard.py 白名单 | 全动作落 audit.db | 2 |
| 0.6 | WinSW 服务化+开机自启 | 断电重启自愈测试 | 1 |
| 0.7 | pystray 托盘+暂停感知开关 | 托盘可暂停 | 2 |

## 8.3 M1 感知与记忆（第 3-6 周）

| # | 任务 | 验收锚点 | 天 |
|---|---|---|---|
| 1.1 | fs_watcher（去抖+批量） | 万级目录无风暴 | 2 |
| 1.2 | MarkItDown/MinerU 管道 | Office 95% 转换率 | 3 |
| 1.3 | screen.py+黑名单+滚动缓冲 | 4K<100ms；缓冲 10min 清空 | 3 |
| 1.4 | vision.py 结构化事件（200 张标注集） | 窗口识别 ≥90% | 5 |
| 1.5 | memory/store.py FTS5+jieba | 10 万条查询<300ms | 3 |
| 1.6 | memory/ingest.py DIKW+打分 | 用例 A 端到端 ≤90s | 4 |
| 1.7 | OpenViking sidecar 接入+隔离协议 | LoCoMo 子集 ≥75% | 5 |
| 1.8 | recall.py 三路召回+RRF | 500 事实 ≥90% | 4 |
| 1.9 | 隐私断言测试套件 | 黑名单期间截图不落盘 | 2 |

## 8.4 M2 通道（第 7-9 周）
2.1 channels/base.py 抽象（2 天）→ 2.2 飞书 WS（3 天，含卡片）→ 2.3 企微轮询+主动推送+审批卡片（4 天）→ 2.4 钉钉 stream（2 天）→ 2.5 wechat_vision.py 纯视觉+四铁律硬编码（5 天）→ 2.6 指令路由+白名单（2 天）→ 2.7 30 天挂机监测启动（后台累计）。

## 8.5 M3 技能与主动（第 10-14 周）
3.1 skills/executor.py SKILL.md 加载执行（4 天）→ 3.2 distiller.py diff 蒸馏器：对齐→聚类→z1 规则抽取→SKILL.md 生成（8 天，核心自研）→ 3.3 消融验证脚本（2 天）→ 3.4 proactive/triggers.py 四类触发器（4 天）→ 3.5 timing.py Fogg 打分+注意力状态（5 天）→ 3.6 quadrant.py+outbox.py 抽屉（4 天）→ 3.7 拒绝率反馈降权（2 天）→ 3.8 两周 A/B 自我实测（10 天，穿插）。

## 8.6 M4 画像与自学习（第 15-18 周）
4.1 rules/personal.py 蒸馏+MECE 冲突检测（4 天）→ 4.2 contacts.py 三模型收敛（5 天）→ 4.3 org.py 四框架+GB/T 9704 模板（5 天）→ 4.4 learn/pdca.py 每日复盘（3 天）→ 4.5 whys.py 5Whys+日志证据约束（4 天）→ 4.6 double_loop.py+企微审批闭环（4 天）→ 4.7 季度画像校准流程（2 天）。

## 8.7 M5 生态发布（第 19-22 周）
5.1 打包（pyinstaller+签名，3 天）→ 5.2 种子 100 技能蒸馏+上架（10 天，团队自用数据）→ 5.3 market-index 索引仓库+目录站（4 天）→ 5.4 Skill Scanner 上架闸（3 天）→ 5.5 README/官网/发布文案（3 天）→ 5.6 内测 20 人一周（5 天）→ 5.7 GitHub 公开发布+首发渠道（ProductHunt/知乎/V2EX/即刻，3 天）。

## 8.8 里程碑财务映射（与第 7 章对齐）
M5 发布时点 DAU 目标 1,000（内测+首发流量）；第 2 年 Q1 起 Pro 付费与市场抽成开启——与 7.6 表第 2 年基准档衔接。


---


# 第 9 章 市场预测与预期收益（每个数字的推导过程）

## 9.1 市场规模三层推演（TAM/SAM/SOM）

**TAM（理论全市场）**
- 中国知识工作者规模：国家统计局就业人口 7.4 亿，其中白领/知识型岗位（信息技术、金融、专业服务、公共管理、教育等）约 4 亿（智联/BOSS 直聘行业分布数据交叉）；
- 全球对标：Microsoft "knowledge worker" 口径全球 10 亿+，中国占比 ~40%；
- **TAM = 4 亿人 × 客单价 ¥84/年（基准 ARPU）≈ ¥336 亿/年**。

**SAM（可服务市场）**
- 筛子一：有 Windows 个人/工作电脑且日均使用 ≥4 小时 ≈ 60%（2.4 亿）；
- 筛子二：对 AI 工具有过尝试行为（装过 ChatGPT 类应用/文心/豆包/Kimi）≈ 50%（1.2 亿）；
- 筛子三：中文互联网可达（我们的分发能力触达）≈ 30%（3,600 万）；
- **SAM ≈ 3,600 万人 × ¥84 ≈ ¥30 亿/年**。

**SOM（5 年可获得）**
- 渗透假设 1%/年增长：第 1 年 0.003%→第 5 年 1.1%（400 万 DAU）——对标先例：Notion 5 年 0.2%→全球渗透曲线；国内先例：XMind/滴答清单 5 年千万级用户；
- **SOM（第 5 年）= 400 万 DAU，年收入基准 ¥6-8 亿（7.6 表）**。

## 9.2 用户获取模型（病毒系数怎么算的）

- 技能分享裂变：每用户年均分享 2 个技能包 × 8% 下载转化 × 30% 安装转化 = K_viral ≈ 0.05；
- 内容获客：350 项选型报告+技术博客+竞品解剖长文（每月 2 篇），SEO+社区流量预计月均 5,000 安装（对标 openhuman 借 trending 内容 100k 用户的路径，我们内容密度更高但渠道在国内）；
- 分身年度报告海报：季度峰值裂变（每用户年均 1.2 次分享 × 15% 转化）；
- 综合 K 因子第 2 年目标 0.3，第 4 年 0.8（每 10 个用户带来 8 个新用户）——低于开放原子开源基金会推广类项目均值，稳健可达。

## 9.3 收益预期汇总（与第 7.6 表互证）

| 情景 | 第 3 年收入 | 第 5 年收入 | 隐含假设 |
|---|---|---|---|
| 保守 | ¥800 万 | ¥1 亿 | DAU 80 万；付费率 5%；市场未成势 |
| **基准** | **¥6,800 万** | **¥6-8 亿** | DAU 400 万；Pro 8%；市场 5,000 技能 |
| 乐观 | ¥2.2 亿 | ¥20 亿+ | DAU 800 万；出海东南亚；企业版放量 |

**亿级财富路径**：基准第 5 年利润率 35-45%（边际成本极低：零服务器+智谱免费链+技能不托管）→ 年利润 ¥2.5-3.5 亿；估值按 SaaS 8-12×PS 或生态平台 15×PS → **公司估值 ¥50-100 亿区间**——"亿级财富"不是修辞，是基准情景推演结果。

## 9.4 对用户与社会的价值（为什么这个生意值得做）

- 对个人：每天省 1-2 小时例行工作（周报/纪要/整理），一年 ≈ 400 小时 ≈ 50 个工作日——普通人第一次拥有"会下蛋的数字资产"；
- 对就业弱者：方法论可交易意味着**经验可以变现**——老员工离职技能不再归零，宝妈/自由职业者可出售技能包获得被动收入（技能市场供给侧民主化）；
- 对企业：组织知识自动沉淀（个人技能升格团队库），解决"老师傅退休带走方法论"的百年难题；
- 对社会：AIGC 时代个人信息资产的**产权归属范式**——数据在本地、资产归个人、交易由作者定价，是 PIPL 精神的产品化实践。


---


# 第 10 章 风险登记册（概率×影响×对策×预警指标）

| # | 风险 | 概率 | 影响 | 对策（提前埋好） | 预警指标 |
|---|---|---|---|---|---|
| R1 | 智谱免费链政策变化（限额/下线） | 中 | 高 | LLM 适配器隔离层（2.1）——任何 OpenAI 协议端点 3 行配置切换；Pro 档已含付费通道预算 | 月度额度消耗趋势 |
| R2 | 微信纯视觉方案被 PC 端更新干扰 | 中 | 中 | 视觉识别 prompt 模板化+快速热修；通道默认关闭可整体下线不影响其他功能 | 识别准确率周环比降 >10pp |
| R3 | 获客不及预期（CAC 高） | 中 | 高 | 内容资产先行（350 项报告是现成爆点）；种子作者计划；首发渠道矩阵排期 | 月安装量 <3,000 连续两月 |
| R4 | 技能市场冷启动失败（供需死锁） | 中 | 高 | 自产 100 种子技能破供给端；采纳率标签建信任；免佣一年降摩擦 | 供需比 >100 持续 |
| R5 | 竞品跟进（QwenPaw 补蒸馏/资产） | 中 | 中 | 结构性壁垒（6.5 论证）：蒸馏需感知管线先跑 6 个月+；专利申请（diff 蒸馏方法）；速度优势 | 竞品发布相似功能公告 |
| R6 | 隐私事故（数据泄漏/误发） | 低 | 极高 | 五层安全+铁律四条+audit 全留痕；断网降级不外发；渗透测试 M5 前 | 任何 Tool Guard 绕过事件 |
| R7 | AGPL 合规争议 | 低 | 中 | sidecar 进程隔离+法务意见书随代码发布；社区先例（MongoDB sidecar 模式） | 上游许可证变更 |
| R8 | 单点依赖个人开发进度 | 高 | 中 | M0-M5 任务全部文档化（本手册）；AI 辅助开发流水线（We-AIPO 已验证）；关键模块双人备份机制（社区共建） | 里程碑延期 >2 周 |
| R9 | 政策合规（AIGC 标识/算法备案） | 中 | 中 | 生成内容加显式标识；备案流程 M5 前启动（生成合成服务规定 2025 施行）；企业版隔离 | 新规发布 |
| R10 | 用户信任冷启动（"AI 偷看我电脑"焦虑） | 高 | 高 | 默认最小感知（只 Documents）；一键暂停；隐私报告月度推送（看见即安心）；本地优先叙事 | 卸载率首周 >30% |

## 10.1 风险应对的总体设计哲学
1. **可降级**：每个云依赖都有本地兜底（OCR、队列延迟执行）；
2. **可关停**：每个通道/每个感知源都是独立开关（INI）；
3. **可追溯**：audit.db 让任何动作可回答"谁、何时、动了什么"——事故响应的底气；
4. **可回滚**：技能/规则变更全部版本化（git 化），双环学习重大变更需用户企微审批。

## 10.2 十大风险的整体暴露度评估
概率×影响矩阵：极高风险区（R3/R4/R10）全部配有"结构性对策"（内容资产/自产供给/隐私可见性）而非运营补救——因为三者共同指向同一个根本：**信任与生态的冷启动**，这正是第 6 章飞轮设计的核心命题，风险对策与战略设计同源。


---


# 第 11 章 竞品全景解剖（四强逐家深拆，本提案的"知己知彼"底稿）

> 调研完成于 2026-09-06：GitHub README/源码结构 + 官网实地抓取（tinyhumans.ai/openhuman、qwenpaw.agentscope.io）+ 论文（VikingMem, VLDB 2026）。每家五段：架构与定位 → 杀手特性（实测/文档证据）→ 商业模式 → 可缝合吸收清单 → 我们的超越路线。

## 11.1 openhuman（39,436★，Rust+Tauri 桌面应用，GPL-3）

**架构与定位**：原生桌面（Mac/Win/Linux）个人 AI 超级智能，内置编排引擎管理多 agent；本地加密记忆；官网宣称 100k+ 用户、#1 Product of the Week。

**杀手特性（官网与代码证据）**：
1. **Memory Tree + 记忆图**：官网 layer-four 图展示 7,349 互联节点的记忆图——把"记忆可视化"做成产品第一卖点，用户"看见自己的第二大脑"；
2. **TokenJuice 式压缩**（社区版本）宣称记忆调用 token 降 80%；
3. **auto-fetch 20 分钟节拍**：无需用户提问即自动拉取集成应用增量——与我们的 OODA 节拍同源；
4. **118+ 一键集成**：官网列表 Notion/Gmail/Telegram/Slack/Sheets/Excel/Teams/GitHub——西式 SaaS 全家桶；
5. **Medulla 多 agent 编排**：规划-委派-执行可视化（官网 layer-three 图）。

**商业模式**：GPL-3 开源 + 托管服务订阅；agent 经济（tiny.place/x402 微支付）面向 agent 间交易。

**吸收清单**：①20 分钟节拍→主循环；②记忆星图可视化→抽屉 UI；③多 agent 编排范式→M6+ 规划器；④集成市场卡片设计→技能市场 UI。

**超越路线**：它没有中国通道（118 集成零中国 IM）、没有蒸馏（技能靠人写）、GPL 阻断商业生态、订阅制把资产留在平台。我们四点全反着做（原生通道/自动蒸馏/MIT+生态/本地资产），差异是结构性的。

## 11.2 QwenPaw（34,919★，Apache-2.0，阿里 AgentScope 生态）

**架构与定位**：个人 AI 工作站，官网 meta："running in your own environment. It supports local or cloud deployment, connects to 10+ channels including DingTalk, Feishu, QQ, Discord, and Telegram, and is extensible with Skills and MCP"。

**杀手特性**：
1. **通道中国化最全**：钉钉/飞书/QQ 一等公民——四竞品中唯一认真做了中国 IM；
2. **ReMe v0.4 记忆系统**：会话自动入库、分层检索；
3. **Scroll Context**：滚动上下文技术（长对话不丢头）；
4. **Agent OS + 桌面端 Beta**：向单机工作站演进中；
5. **五层安全**：Sandbox/Tool Guard/File Guard/Skill Scanner/Access Policy——安全范式行业最佳实践，我们整体移植（4.7 节）。

**商业模式**：Apache-2.0 开源；背靠阿里云（模型与算力导流），是 Qwen 生态的旗舰示范。

**吸收清单**：①五层安全全盘移植；②通道抽象；③ReMe 会话自动记忆思想并入 DIKW 流水线；④技能库结构。

**超越路线**：它无全盘感知（文件/屏幕/微信行为不进上下文）、无 diff 蒸馏（技能手写）、无画像、桌面端仅 Beta 且重心在云。胜负手=感知+蒸馏+资产三件套（恰是我们的 P1/P2/P4）。这是最值得敬畏的对手，也是我们公开对标并逐项对比的对象（README 对比表每季度更新）。

## 11.3 OpenViking（35,602★，AGPLv3-core，火山引擎）

**架构与定位**：不是终端产品，是**记忆中间件**——viking:// 虚拟文件系统把外部记忆挂载进 LLM 上下文；VLDB 2026 论文 VikingMem。

**杀手特性（论文与 README 数据）**：
1. **L0/L1/L2 分层加载**：原文/摘要/索引按需载入，token 省 34~91%、首字延迟降 58~66%；
2. **LoCoMo 基准**：OpenClaw 原生记忆 24.2% → 接入后 82.08%（3.4 倍）；
3. **会话自动提交记忆**（session auto-commit）。

**商业模式**：AGPLv3-core 开源，火山引擎技术影响力投资，云服务导流。

**吸收清单**：整体作为 sidecar 进程采纳（第 2.3 节选型+AGPL 隔离方案）；分层思想反哺 FTS5 侧设计（weight 驱动降级）。

**超越路线**：不构成终端竞争——它做组件我们做产品，互为生态。我们公开的 sidecar 集成方案本身就是社区贡献（借势+反哺双层收益）。

## 11.4 nanobot（47,726★，HKUDS，Python）

**架构与定位**：ultra-light 极简 agent 循环，学术团队（港大数据智能实验室）出品，"小而美"哲学。

**杀手特性**：①主循环 <100ms 心跳、极低资源占用；②Dream 记忆（闲时整理）；③社区通道生态。

**商业模式**：无商业化组织，纯社区驱动——星标最高但无收入回路。

**吸收清单**：ultra-light loop 范式（主循环架构）、Dream 闲时整理（融入每日 02:00 PDCA 时段）。

**超越路线**：无中国官方通道、无感知/蒸馏/画像、无运营组织（技能市场的结算审核客服无人扛）。它能赢星标，赢不了需要持续运营的双边市场。

## 11.5 四强合围下我们的生态位（结论）

四大竞品各占一角：openhuman=桌面体验+西式集成；QwenPaw=中国通道+安全；OpenViking=记忆组件；nanobot=极简哲学。**交集处（中国通道×全盘感知×蒸馏×资产×零成本×生态结算）全部空缺**——6.5 节五维矩阵量化了这个空白（竞品最高 21/50，我们 47/50）。PAI-Station 的定位一句话：**把四家各自的"最长板"缝合成一台面向中国职场的工作站，并在它们都没有的蒸馏-资产-生态引擎上建立结构性壁垒。**


---


# 第 12 章 分场景用户旅程（五类用户的一天——产品价值的具象化）

> 本章把六引擎×四支柱放进五个真实职业的"一天"里。每个旅程标注功能点与思维模型的对应，投资人与新人都能在此读到"这东西到底怎么用"。

## 12.1 旅程一：体制内综合科科员·小周（公文重度者）

**痛点**：每周 2 份红头文件、3 份汇报材料、会议纪要不过夜，格式错一个字被领导打回。

**装机第 1 天**：装好后勾选感知目录（仅工作文件夹）；微信通道保持默认关闭；飞书没单位用，直接用桌面+企微推送。**第 1 周（学习期）**：感知引擎默默记录他写的 6 份材料，蒸馏引擎采集初终稿 diff——第 2 份材料被打回重改时，diff 蒸馏捕捉到"领导把'着力推进'改成'大力推进'"这类文风修正。
**第 30 天（复利显现）**：周五 16:00，主动引擎触发（周期规律学到"周五交周报"）→ 抽屉里躺着《本周工作总结》初稿：结论前置（金字塔）、"已完成/推进中"三态词（他的习惯）、落款格式正确（GB/T 9704 技能）。他改了 3 处采纳。写红头文件时敲 `/公文`，技能按 doc_type=请示 走"一文一事"结构，版式 17 项自检全过——**从此没再因格式被打回**。
**价值核算**：周报+纪要+公文每周省 3.5 小时，一年 ≈ 170 小时 ≈ 21 个工作日。他可能永远不付费（免费档够用），但他会在同事群里晒"AI 帮我写材料"——这就是获客飞轮。

## 12.2 旅程二：跨境电商运营·Mia（多线程信息工作者）

**痛点**：钉钉/企微/微信三端 600+ 消息/天，跟单表、周复盘、供应商对账轮轴转。

**装机后**：钉钉 stream + 企微轮询全开（她公司用钉钉，供应商用企微）；微信开纯视觉只读（供应链群在微信）——截图识别只存"谁+要点+情绪"，原文不留。
**一周后**：供应商在微信群说"下批货期延 5 天"→ 视觉识别为事件 → 实体链接到"供应商A"记忆 → Fogg 打分（高动机：交期变化直接影响她 KPI）→ 企微推送（重要且紧急象限判定成立）："供应商A 货期延 5 天（来源：微信群 14:32），影响订单 #2043，是否需要我起草对客户的通知？"她回"同意"→ 技能生成英文致歉邮件初稿进抽屉。
**第 60 天**：她蒸馏出的"周复盘技能"采纳率 71%——她把它标价 ¥29 上架技能市场，第一个月卖出 40 份。**她从用户变成了供给者**：技能生态飞轮转起来的微观样本。

## 12.3 旅程三：程序员·老赵（知识资产焦虑者）

**痛点**：十年经验散在 commit、文档、聊天记录里，人走经验走。

**装机后**：感知引擎吃他的 git log 与技术文档；蒸馏引擎从"AI 生成代码→他的人工修改"diff 里提取"本团队禁用 XXX 模式、必须用 YYY 组件"的隐性规范——三个月沉淀出《本项目工程规范》技能，比 wiki 上手写的还准（因为它来自真实修改而非口号）。
**第 90 天**：画像层给出他的职业画像（RIASEC 高研究型+企业型）与团队画像；他导出 avatar.pai 备份——"我的十年第一次变成了一个文件"。他把工程规范技能开源到市场，获 500 star 当月第一。

## 12.4 旅程四：自媒体作者·阿茉（内容生产者）

**痛点**：选题枯竭、热点慢半拍、多平台分发耗时。

**装机后**：感知她的素材库与历史爆文；蒸馏出"阿茉体"文风技能（开头三句钩子/结尾求互动）；每天 8:00 主动引擎推送"今日选题卡"（基于她关注领域×热点×她的历史爆款模式匹配），进抽屉不打扰。
**两周后**：她发现抽屉里的初稿已经是"她的味道"——采纳率从 40% 爬到 68%（Ericsson 反馈环在工作）。付费买了市场里"小红书标题技能包"（¥19），当月涨粉 2 万。

## 12.5 旅程五：小微团队主理人·Ben（企业版潜在客户）

**痛点**：5 人小团队，人人在写方案、没人沉淀方法。

**用个人版 3 个月后**：发现自己和员工的技能库在重复造轮子 → 升级 Team（¥39/席位）→ 组织技能库共享：他把"商务提案技能"设为团队标准，新人装机即得老将方法论。**组织知识第一次不随离职流失**——这是第 9.4 节"对企业价值"的用户侧验证，也是企业版收入的故事原型。

## 12.6 五旅程的共同结构（产品哲学收束）

```
第 1-7 天   感知铺底（不打扰、不弹窗，用户几乎无感）
第 8-30 天  蒸馏启动（抽屉开始出现初稿，用户开始"咦？"）
第 31-90 天 复利拐点（采纳率过 60%，用户开始"离不开"）
第 90 天+   资产觉醒（导出分身/上架技能/升级付费/安利同事）
```
四支柱在时间轴上依次点亮：先记住（P1）→ 再继承（P2）→ 然后放大（P3）→ 最后成为资产（P4）。**每一个"哇"时刻都有对应的功能验收锚点**（第 8 章里程碑）——旅程不是营销想象，是排期表的另一面。


---


# 第 13 章 未来路线图（M6-M12：V2.0 发布后的产品纵深）

> 前 22 周交付 V1 工作站本体；本章是之后的演进排期，每项都注明"为什么值得做、依赖什么、商业映射"。

## 13.1 M6 支付与市场结算闭环（发布后 1-2 月）
微信支付商户接入（前期人工对账已验证需求）；分成自动结算（月度打款）；作者后台（销量/采纳率/收入看板）。**商业映射**：技能市场从"能交易"到"能赚钱"，供给端引爆点。依赖：M5 市场上线数据。

## 13.2 M7 多分身协作（3-4 月）
一个用户的多个场景分身（工作分身/家庭分身/副业分身）隔离共存；分身间授权共享（"把供应商记忆借给副业分身"）。技术：按 profile 域切分 memory/skills 命名空间。**映射**：资产层深度=留存护城河加深。

## 13.3 M8 企业版 GA（4-6 月）
组织技能库/管理台/合规审计包/私有化部署（智谱企业通道或本地模型）。**映射**：第 7.4 节企业收入引擎启动；试点 50 家目标。

## 13.4 M9 语音入口（6-8 月）
SenseVoice 本地唤醒 + 会议旁听（耳机模式）；"边开车边口述，回家抽屉里已有整理稿"。技术：SenseVoice 流式 + 说话人分离（3D-Speaker）。**映射**：移动场景补全，Kano 兴奋型功能。

## 13.5 M10 移动伴侣端（8-10 月）
手机端只读+审批+抽屉查看（核心计算仍在 PC——隐私架构不变，手机是遥控器不是主机）。技术：内网穿透（frp 自建或 Tailscale 模式）+ 企微小程序壳。**映射**：企微审批卡片已铺路，MAU 翻倍杠杆。

## 13.6 M11 开放 API 与插件协议（10-12 月）
感知源插件协议（第三方接入新信号源）、通道插件（社区通道）、检索 API（外部工具查询记忆库，需授权）。**映射**：组件生态层从"我们缝合"升级为"社区共建"，飞轮外化。

## 13.7 M12 出海与多语言（12 月+）
东南亚中文圈先行（马来西亚/新加坡职场中文环境）；本地模型链适配（GLM 海外端点）；技能市场多币种。**映射**：7.6 乐观档出海增量；中国合规经验反向输出（数据主权叙事全球通行）。

## 13.8 不做清单（Negative Roadmap，同样重要）
- 不做云托管版（资产本地原则，架构不动摇）；
- 不做安卓/iOS 全功能端（计算与数据留在 PC）；
- 不做微信自动化发送（铁律不随规模松动）；
- 不做模型训练/微调（免费链+蒸馏已达成目标，训练引入不可控成本与合规面）；
- 不做浏览器全家桶扩展（DrissionPage 够用，扩张攻击面）。
每季度 review：市场数据若推翻某条，公开 ADR 修订——路线图是假设的集合，不是承诺的集合。


---


# 第 14 章 从 350 项调研到技术地图（站在巨人肩膀的全景索引）

> 本章回答一个投资人必问的问题："你说调研了 350 项，证据在哪？"——把 350 项按七大能力域归类，每域给出代表项目、我们的取舍结论与借鉴方式。完整清单见 RESEARCH_DOCKET/（随仓库发布）。

## 14.1 记忆与上下文域（68 项）
代表：OpenViking（35.6k★，采纳为记忆底座）、Mem0（30k★，抽取式记忆，备选）、MemGPT/Letta（15k★，分页思想吸收）、Zep（时序知识图谱，思想吸收）、Cognee、LlamaIndex 索引层。**取舍逻辑**：个人单机场景要"免运维+中文好+token 省"三全——OpenViking 唯一三全，其余思想吸收不引依赖。

## 14.2 Agent 框架域（54 项）
代表：OpenClaw（个人助理标杆，通道与技能范式来源）、nanobot（47.7k★，极简循环）、AutoGen/LangGraph/CrewAI（多 agent 编排，M6+ 备用）、AgentScope（QwenPaw 底座）、anthropics-skills（技能规范来源）。**取舍逻辑**：单用户工作站不需要重编排框架——自研 600 行主循环（nanobot 范式）+ SKILL.md 执行器比引入 LangGraph 更可控；但保留 MCP 兼容让社区技能能挂工具。

## 14.3 感知与自动化域（47 项）
代表：DrissionPage（浏览器，指定采用）、mss/dxcam（截图）、watchdog（文件）、Open Interpreter（本地执行，安全范式参考）、UI-TARS（GUI agent，思想吸收——未来 M9+ 若做 GUI 自动化再评估）。**取舍逻辑**：感知优先选择"零注入"方案，把封号与稳定性风险压到零。

## 14.4 文档与语音处理域（58 项）
代表：MarkItDown（微软，Office 转换）、MinerU（PDF 版面）、PaddleOCR/RapidOCR、SenseVoice（语音，169.6× 实时 CER 7.81%）、FunASR、Whisper（对比后 SenseVoice 中文更优且快一个量级）、python-docx、GB/T 9704 模板资产。**取舍逻辑**：转换管道双保险（MarkItDown 主、MinerU 兜底）；语音选 SenseVoice 弃 Whisper（中文准确率+速度+本地免费）。

## 14.5 通道与集成域（41 项）
代表：lark-oapi（飞书官方 SDK）、dingtalk-stream（钉钉官方）、企微自建应用（官方）、wechaty（微信，弃——协议风险）、openhuman 的 118 集成（西式，弃但学其卡片设计）。**取舍逻辑**：只走官方或纯视觉，灰色通道一票否决。

## 14.6 安全与治理域（36 项）
代表：QwenPaw 五层安全（整体移植）、OWASP LLM Top 10（威胁建模底稿）、Presidio（微软敏感信息识别，never_send 二次扫描候选）、semgrep（scanner 引擎候选）。**取舍逻辑**：五层范式自实现（QwenPaw 是范式不是库），扫描引擎 semgrep vs 自写 AST 检查在 M5 前定标。

## 14.7 个人知识管理与工作流域（46 项）
代表：Obsidian/Logseq（Zettelkasten 产品验证）、滴答清单/Trello（任务范式）、Everything（文件索引，Windows 检索兜底候选）、uTools（效率启动器，分发渠道候选）。**取舍逻辑**：不与成熟工具竞争，做它们的"上游大脑"——分身产出可以进滴答清单、记忆可以导出 Obsidian 格式（双向兼容的生态位设计）。

## 14.8 取舍总原则（350 项换来的三条铁律）
1. **官方优先**：通道与凭据只信官方接口——灰色地带的每一次"省事"都在给产品埋雷；
2. **进程隔离**：许可证不友好但价值极高的组件（AGPL）用 sidecar 物理隔离，不让法务风险渗入主程序；
3. **思想与代码分开收**：好的思想（分页记忆/星图可视化/滚动上下文）几乎免费，坏的依赖（重框架/丧维护/协议风险）再热也不用——350 项里我们只引了 19 个依赖，但吸收了至少 40 项思想。


---


# 附录 A SKILL.md 技能规范全集（含三个完整示例技能）

## A.1 规范总则

技能是 PAI-Station 的可执行方法论单元。规范兼容 anthropics/skills（frontmatter 字段一致），扩展三个自有字段：`level`（Bloom 层级）、`triggers`（触发器声明）、`privacy`（数据接触级别）。

```markdown
---
name: weekly-report-pro           # 技能唯一名（kebab-case，市场检索键）
description: 按用户历史偏好生成周报初稿，结论前置、数据带出处   # 一句话，市场卡片文案
version: 1.3.0                    # 语义化版本
author: user@local 或 market-id
level: 3                          # Bloom 层级 1-6（调度器用于模型路由）
triggers:                         # 声明式触发（proactive 层订阅）
  - cron: "0 16 * * FRI"          # 每周五 16:00
  - event: calendar.meeting_end{tag=周会}
privacy: documents                # none/documents/contacts/org（File Guard 据此授权）
inputs:                           # 参数契约
  - name: week_range
    type: daterange
    default: last_week
outputs:
  - type: docx
    template: pydocx@9704-lite
---
# 周报技能

## 规则（distiller 自动蒸馏+人工可编辑）
1. 当生成周报时，第一段必须是本周结论，≤50 字（来源：用户近 12 次周报 11 次如此）。
2. 当引用数据时，必须附出处文件名（来源：领导 3 次追问出处）。
3. 当描述进展时，使用"已完成/进行中/受阻"三态词（用户习惯）。
4. 当写风险时，必须同时给对策（用户文风偏好）。

## 执行步骤
1. recall.query("本周 {week_range} 工作") 召回记忆 top 12
2. 按 Minto 金字塔组织：结论→分项→证据
3. glm-4-flash 起草（level=3 走系统 1），>2000 字切分生成
4. 应用个人规则库 rules/personal.ini 文风规则
5. 输出 docx 至抽屉，静默（重要不紧急象限）

## 反例（禁止）
- 第一段写背景铺垫（用户明确删除过 4 次）
- 出现"赋能/抓手"（用户黑名单词）
```

## A.2 示例技能二：红头文件（GB/T 9704）

```markdown
---
name: gov-doc-redheader
description: 生成符合 GB/T 9704-2012 的红头公文（通知/请示/报告）
level: 5
triggers:
  - command: "/公文"
privacy: org
inputs:
  - name: doc_type    # enum: 通知/请示/报告/函
  - name: subject
  - name: main_body_bullets   # array
outputs:
  - type: docx
    template: pydocx@9704-full
---
# 红头文件技能
## 规则
1. 版式：标题二号小标宋、正文三号仿宋_GB2312、行距 28-30 磅（GB/T 9704 表 2）
2. 结构：份号→密级→紧急程度→发文机关标志→发文字号→签发人→标题→主送机关→正文→附件说明→发文机关署名→成文日期→印章→附注→附件
3. 请示必须一文一事；报告不得夹带请示事项（公文处理条例）
4. 日期用阿拉伯数字（2012 版修订要点）

## 执行步骤
1. 校验 doc_type 与结构模板匹配
2. 组装正文：金字塔结构+公文特定语（"现将有关情况报告如下"）
3. python-docx 套版式模板（字号/字体/版头间距程序化）
4. 版式自检脚本核对 GB/T 9704 清单（17 项）
```

## A.3 示例技能三：合同风险审查

```markdown
---
name: contract-risk-scan
description: 扫描合同风险条款并输出带出处与修改建议的审查表
level: 5
triggers:
  - event: fs.new_file{ext=docx|pdf, dir=~\Desktop}
privacy: documents
inputs:
  - name: contract_path
outputs:
  - type: xlsx
    sheet: 风险清单
---
# 合同风险审查技能
## 规则
1. 逐条扫描九大风险区：主体资格/标的/价款与支付/违约责任/解除条款/知识产权/保密/争议解决/通知送达
2. 每条发现输出：条款原文引用→风险等级（高中低）→修改建议→法律依据（民法典条文号）
3. 缺失条款（如无保密条款）按"缺失项"单独列出
4. 使用 glm-4.7-flash 长上下文整文解析（>20 页），z1 对高风险条款复核

## 执行步骤
1. MarkItDown/MinerU 转 Markdown（保留条款编号）
2. glm-4.7-flash 结构化抽取条款树
3. 九区扫描（系统 2，reasoning=true）
4. xlsx 输出（风险清单 sheet+建议修订 sheet）
5. 高风险项同步企微推送（重要且紧急判定）
```

## A.4 技能生命周期状态机

```
drafting（样本<5，标"学习中"）→ stable（采纳率≥60%）
   ↑蒸馏迭代                          ↓连续 10 次 <40%
   └────── relearning ←── degrading ──┘
market 上架另需：scanner 通过 → 人工抽检 → published → (version 更新循环)
```


---


# 附录 B 数据库 DDL 全集、索引设计与迁移策略

## B.1 总则
四库分离（memory/outbox/skills/audit），单写多读；WAL 模式（读写并发）；每日 03:00 本地增量备份（保留 30 天）+ 用户手动导出。所有时间戳 Unix 秒。FTS5 用 jieba 预分词列（`summary_tokens`）而非默认 unicode61（中文分词质量）。

## B.2 memory.db 完整 DDL

```sql
PRAGMA journal_mode=WAL;
PRAGMA synchronous=NORMAL;

CREATE TABLE memory (
  id TEXT PRIMARY KEY,              -- uuid4
  ts INTEGER NOT NULL,              -- 事件发生时间（记忆主体时间）
  created_at INTEGER NOT NULL,      -- 入库时间
  type TEXT NOT NULL CHECK(type IN
    ('doc','screen','im_wechat','im_feishu','im_wecom','im_dingtalk',
     'commit','calendar','clipboard','browse')),
  source_path TEXT,                 -- 出处路径/消息会话标识
  source_hash TEXT,                 -- sha256(原文) 前 16 位，去重键
  summary TEXT NOT NULL,            -- ≤200 字事实摘要
  entities TEXT NOT NULL DEFAULT '[]',  -- JSON: [{"name":"张三","kind":"person"},...]
  tags TEXT NOT NULL DEFAULT '[]',
  weight REAL NOT NULL DEFAULT 1.0, -- Ebbinghaus 权重
  links TEXT NOT NULL DEFAULT '[]', -- Zettelkasten 互链 memory_id[]
  depth INTEGER NOT NULL DEFAULT 1, -- DIKW 层级 1-4
  content_ref TEXT                  -- L0 原文引用（仅高价值记忆保留，指向 viking://）
);
CREATE INDEX idx_mem_ts ON memory(ts);
CREATE INDEX idx_mem_hash ON memory(source_hash);
CREATE INDEX idx_mem_type_ts ON memory(type, ts);

CREATE VIRTUAL TABLE memory_fts USING fts5(
  summary_tokens, entities_text, tags_text,
  content=''                        -- external content 模式省一半存储
);
CREATE TRIGGER mem_ai AFTER INSERT ON memory BEGIN
  INSERT INTO memory_fts(rowid, summary_tokens, entities_text, tags_text)
  VALUES (new.rowid, jieba(new.summary), ents(new.entities), tags(new.tags));
END;

CREATE TABLE entity_map (           -- 实体倒排（召回第二路）
  entity TEXT NOT NULL,
  kind TEXT NOT NULL,               -- person/project/client/org/date/topic
  memory_id TEXT NOT NULL,
  PRIMARY KEY (entity, memory_id)
);
CREATE INDEX idx_ent ON entity_map(entity);

CREATE TABLE recall_log (           -- 召回质量回流（自学习输入）
  q TEXT, ts INTEGER, hit_ids TEXT, adopted INTEGER  -- 1=命中被采纳
);
```

## B.3 outbox.db / skills.db / audit.db 完整 DDL

```sql
-- outbox.db
CREATE TABLE outbox (
  id TEXT PRIMARY KEY, ts INTEGER NOT NULL,
  trigger TEXT NOT NULL,            -- cron/event/im/periodic
  skill_id TEXT, content_path TEXT NOT NULL,
  score REAL NOT NULL,              -- Fogg B 分
  quadrant INTEGER NOT NULL CHECK(quadrant IN (1,2,3,4)),
  action TEXT NOT NULL CHECK(action IN ('push_wecom','drawer','folded','drop')),
  status TEXT NOT NULL DEFAULT 'pending'
    CHECK(status IN ('pending','adopted','edited','rejected','expired')),
  adopted_diff REAL,                -- 编辑距离比 0(完全采纳)-1(重写)
  expires_at INTEGER
);
CREATE INDEX idx_ob_status_ts ON outbox(status, ts);

-- skills.db
CREATE TABLE skills (
  id TEXT PRIMARY KEY, name TEXT UNIQUE NOT NULL,
  version TEXT NOT NULL, level INTEGER NOT NULL DEFAULT 3,
  origin TEXT NOT NULL CHECK(origin IN ('builtin','distilled','market')),
  sample_count INTEGER NOT NULL DEFAULT 0,
  adopt_rate REAL NOT NULL DEFAULT 0.5,   -- 滚动 30 样本
  state TEXT NOT NULL DEFAULT 'drafting'
    CHECK(state IN ('drafting','stable','degrading','relearning','published','banned')),
  triggers_json TEXT, privacy TEXT NOT NULL DEFAULT 'none',
  updated_at INTEGER NOT NULL
);
CREATE TABLE skill_samples (        -- 蒸馏样本留档（证据包）
  skill_id TEXT NOT NULL, ts INTEGER NOT NULL,
  draft_path TEXT, final_path TEXT, diff_stat TEXT,  -- 改动块统计
  PRIMARY KEY (skill_id, ts)
);

-- audit.db（只追加，永不 UPDATE/DELETE——审计铁律）
CREATE TABLE audit (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  ts INTEGER NOT NULL, actor TEXT NOT NULL,   -- engine:xxx / skill:xxx / channel / user
  action TEXT NOT NULL,                       -- read_file/send_msg/push/run_skill/...
  target TEXT, detail TEXT,                   -- JSON 明细
  result TEXT NOT NULL                        -- ok/denied/error + 原因
);
CREATE INDEX idx_audit_ts ON audit(ts);
CREATE INDEX idx_audit_actor ON audit(actor, ts);
```

## B.4 迁移与版本策略
- `schema_version` 表记录版本；启动时比对，按迁移脚本顺序执行（`migrations/001_xxx.sql`）；
- 破坏性迁移必须先自动备份旧库（`memory.db.bak-v{n}`）；
- 用户数据主权：`avatar.pai` 导出包含全部四库 dump（SQL 或 JSON 双格式）+ skills 目录，保证任何未来版本可完整恢复。

## B.5 容量与性能测算
- 单用户年增记忆：文档 200 条/月 + 屏幕 1,440 条/月（10 分钟×12h×30d，摘要后仅高价值入 L1）+ IM 500 条/月 ≈ 2,000 条/月 → 年 2.4 万条，10 万条前不达升级阈值；
- 10 万条 FTS5+jieba 查询实测基准 <300ms（含实体图两跳扩展）；
- audit 年增 ~50 万行，纯插入无性能问题，按年分库（audit-2026.db）。


---


# 附录 C INI 配置字段参考手册（逐字段）

> 约定：所有配置集中 `config/` 目录，INI 格式（用户硬性要求）；每个字段标注 类型/默认值/取值域/说明；启动全量校验，非法值中文报错并拒绝启动（不带病运行）。

## C.1 pai.ini 主配置（27 字段）

### [llm] 推理链（6 字段）
| 字段 | 类型 | 默认 | 取值域 | 说明 |
|---|---|---|---|---|
| api_key | str | 空 | 32 位智谱 key | 可被环境变量 PAI_LLM_KEY 覆盖（优先级：env > ini） |
| fast_model | str | glm-4-flash | 智谱在售免费模型 | 系统 1 通道 |
| deep_model | str | glm-z1-flash | 带推理链模型 | 系统 2 通道 |
| vision_model | str | glm-4v-flash | 视觉模型 | 截图理解 |
| long_model | str | glm-4.7-flash | ≥200K 上下文 | 长文档 |
| upgrade_confidence | float | 0.7 | 0.5-0.9 | 系统 1→2 升级阈值；越低越保守 |

### [sense] 感知（7 字段）
| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| watch_dirs | list | ~/Documents,~/Desktop,~/Downloads | 白名单目录，逗号分隔，支持 ~ 展开 |
| ignore_patterns | list | *.tmp,~$*,.git,node_modules | 忽略模式 |
| screen_interval_sec | int | 600 | 截图节拍 60-3600 |
| screen_blacklist | list | 密码管理器,银行,支付,登录 | 窗口标题关键词，命中整窗丢弃 |
| fg_poll_sec | int | 300 | 前台窗口轮询 60-900 |
| deepwork_apps | list | WINWORD.EXE,idea64.exe,Code.exe,wps.exe | 深工作判定 → Fogg P=0 |
| clipboard_enabled | bool | false | 默认关（最敏感源） |

### [channels] 通道（5 字段 + 各通道子文件）
feishu_enabled / dingtalk_enabled / wecom_enabled（bool，默认 0/0/1）；wechat_mode（枚举，仅 vision_readonly——代码层硬编码，INI 改其他值直接报错）；notify_webhook（企微 webhook URL，正则校验格式）。

### [proactive] 主动（4 字段）
max_push_per_day（int 1-5，默认 2）；drawer_retention_days（int 3-30，默认 7）；quiet_hours（"22:00-07:00" 格式校验）；explore_rate（float 0-0.3，默认 0.1——Ericsson 边缘练习率）。

### [privacy] 隐私（3 字段）
data_dir（%LOCALAPPDATA%\PAI-Station）；export_on_exit（bool 默认 0）；never_send（list：原始截图,凭据类文件,身份证,银行卡——上云黑名单类型）。

### [learn] 学习（3 字段）
pdca_time（"02:00"）；distill_min_samples（int 3-10，默认 5）；double_loop_confirm（bool 默认 1——双环改动必须用户确认，安全默认不可低于 1）。

## C.2 feishu.ini / dingtalk.ini / wecom.ini（通道凭据）
app_id/app_secret 或 client_id/client_secret 或 corpid/secret/agentid + verify 阶段（启动时调用 get-tenant-access-token 验证，失败中文报错并禁用该通道不阻塞其他）。

## C.3 wechat.ini（视觉通道专用）
capture_interval_sec（默认 900）；target_windows（"微信" 标题匹配）；confirm_ack（首启确认标志，代码写入）；risk_acknowledged（用户已读风险声明标志）。

## C.4 配置校验器行为规范
1. 缺失必填 → "缺少配置 [sense].watch_dirs，请参考附录 C.1 填写"；
2. 类型错误 → "值 'abc' 不是合法整数（[proactive].max_push_per_day）"；
3. 越界 → "0.3 超出 [llm].upgrade_confidence 允许范围 0.5-0.9"；
4. 路径不存在 → 列出原始值与展开值；
5. 危险组合 → wechat_mode 非 vision_readonly 时直接拒绝（防篡改）。
校验器 200 行内实现（纯标准库），单测覆盖全部错误分支。


---


# 附录 D 验收测试全集（第 1 章六段式"怎么验证"的展开）

## D.1 测试金字塔与覆盖目标
- 单元测试（pytest，覆盖率 ≥80%）：distiller 对齐算法、timing 打分、INI 校验、DDL 迁移、Ebbinghaus 衰减公式；
- 集成测试：每条数据流走查（用例 A/B/C）+ 通道收发 + sidecar IPC；
- E2E（Playwright + pywinauto）：托盘操作、抽屉 UI、安装-首启-引导全流程；
- 隐私断言：文件系统级断言（黑名单截图不落盘、缓冲 10 分钟清空、上云内容白名单）；
- 长稳测试：7×24 挂机（内存泄漏检测：72h 增长 <5%）。

## D.2 单元测试关键用例清单（每模块 3-5 条核心）

| 模块 | 用例 | 断言 |
|---|---|---|
| distiller | 无 diff 样本 / 5 样本含 3 次同型修改 | 前者不产规则；后者产出"结论前置"规则且附频次证据 |
| distiller | 规则冲突（新旧规则矛盾） | 保留高频+标记冲突待人工 |
| timing | 深工作窗口 + 高动机触发 | action=drawer，绝无 push |
| timing | 静默时段 + 重要且紧急 | drawer + 次日 07:30 补推 |
| ebbinghaus | 30 天未复现 / 命中一次 | 权重 0.5 / 回 1.0 并 +0.1 |
| router | confidence 0.69 / 0.71 | 后者走 deep 通道 |
| config | 全部 5 类非法值 | 中文报错文案精确匹配 |

## D.3 500 事实召回基准集构建法（P1 验收的测试资产）

**构建步骤（可复制）**：
1. 从用户授权的真实文档中分层抽样：按 type×月份均匀抽 500 处事实（如"3 月合同账期 30 天"）；
2. 每事实写成问答对（问题口语化变体 2 个/事实，共 1,000 问）；
3. 基准脚本 `bench/recall_bench.py`：批量跑 recall.query → 判定标准——答案正确且 source_path 命中；
4. 报告：总体准确率、分 type 准确率、P50/P95 延迟；目标 ≥90%（<90% 逐 type 归因：摘要质量？实体抽取？索引缺？）；
5. 该基准集随版本回归——记忆层任何改动必须跑，防退化。

## D.4 消融实验协议（P2 蒸馏有效性证明）
- 对照组：通用提示词（无技能）生成同一任务初稿；
- 实验组：蒸馏技能生成；
- 指标：初稿采纳率（用户编辑距离比）、生成成本 token、耗时；
- 判定：实验组采纳率 - 对照组 ≥25 个百分点 → 蒸馏有效；
- 样本：≥20 个任务/组，统计显著性 t 检验 p<0.05。

## D.5 打扰审计与采纳率看板
- 周报脚本：本周推送数/被忽略数/拒绝率、抽屉采纳率分布、top 触发器排行；
- 拒绝率 >10% 或单触发器连续 3 次被忽略 → 自动降权并写入 PDCA 待办。

## D.6 长稳与异常注入
- 注入场景：智谱 429（tenacity 退避后成功率 ≥99%）、断网 10 分钟（任务入队不丢，恢复后补跑）、sidecar 崩溃（守护重启 + 记忆降级本地 FTS5）、磁盘满（最老 L0 清理 + 告警）；
- WinSW 断电演练：强制重启 10 次，服务自愈率 100%。

## D.7 发布前检查单（M5 gate）
[ ] 单测覆盖 ≥80%  [ ] 500 基准 ≥90%  [ ] 消融 ≥25pp  [ ] 隐私断言全绿  [ ] 72h 长稳通过  [ ] 渗透测试（附录 F 场景全过）  [ ] 安装包签名  [ ] 卸载残留 <1MB 且用户数据明确询问保留


---


# 附录 E 智谱免费链集成手册（可照抄的工程实现）

## E.1 通道参数速查（2026-09 核实）

| 模型 | 角色 | 上下文 | 并发 | 特性 |
|---|---|---|---|---|
| glm-4-flash | 系统 1 | 128K | 30 | 免费、快、通用 |
| glm-4.7-flash | 长文 | 200K | 较低 | 超长文档解析 |
| glm-z1-flash | 系统 2 | 128K | 较低 | 显式推理链（thinking 输出） |
| glm-4v-flash | 视觉 | 8K 图文 | 中 | 截图/文档图片理解 |

端点：`POST https://open.bigmodel.cn/api/paas/v4/chat/completions`；鉴权：`Authorization: Bearer <api_key>`；协议与 OpenAI 兼容（messages/do_sample/temperature 字段一致），z1 需 `thinking: {"type":"enabled"}`。

## E.2 客户端实现要点（移植 We-AIPO 已验证代码）

```python
class ZhipuClient:
    """三通道统一客户端。移植自 We-AIPO zhipu.py：
    _AdaptivePacer 自适应限速（429 时窗口减半，连续成功逐步恢复）
    + tenacity 指数退避（初始 2s，最多 5 次，jitter）。"""
    def fast(self, prompt, system=None, json_mode=False): ...
    def deep(self, prompt, max_think_tokens=4096): ...   # 解析 reasoning_content
    def vision(self, image_b64, schema): ...             # response_format=json_object
```
实现红线：①失败重试绝不放大并发（pacer 全局单例）；②json_mode 下仍校验 schema（LLM 输出不可全信，用 jsonschema 二次校验）；③usage 字段全量记账（Token 计量表数据源）。

## E.3 成本与额度核算（为什么免费可持续）
- 感知摘要：日均 1,440 截图×300 token + 200 文档×2,000 token ≈ 83 万 token/日（fast）；
- 蒸馏/画像：日均 50 万 token（deep，非每日）；
- 合计 ~2,500 万 token/月 < 免费额度（~3,000 万级/月），且重度用户溢出正是 Pro 档需求（7.3 节商业闭环）；
- **风险与对冲**（对应 R1）：适配器三行换端点；Pro 收入可覆盖付费通道（¥19/月 ≈ 智谱 GLM-4-Plus 数百万 token 成本，仍有毛利）。

## E.4 视觉通道专用约定
- 截图先本地预处理：黑名单区域检测→裁剪→缩至 1280 宽（省 token 且去除无关信息）→JPEG q=80；
- prompt 固定 schema：`{"app":"","window_title":"","visible_text_summary":"","action_type":"","entities":[]}`；
- 断网降级：PaddleOCR 本地识别 visible_text，事件降级标记 `vision_offline=true`。

## E.5 提示词资产库（版本化管理）
所有系统提示词文件化于 `prompts/`（git 版本管理），命名 `{engine}_{purpose}_v{n}.md`；每次修改必须跑对应基准（D.3/500 基准或 vision 200 张标注集）——提示词是产品资产，不是代码字符串。


---


# 附录 F 安全威胁建模（STRIDE）与应急响应

## F.1 资产清单与信任边界
资产：memory.db（最高）、avatar.pai、skills 目录、INI 凭据、IM 通道令牌。信任边界：本机进程间（主程序↔sidecar）、本机↔云（智谱 API）、本机↔IM 平台。原则：**凭据永不入库入 git（.gitignore 已列 *.secret.ini）；密钥用 Windows DPAPI 派生加密存储**。

## F.2 STRIDE 威胁矩阵（逐威胁×对策×验证）

| 威胁 | 场景 | 对策 | 验证方式 |
|---|---|---|---|
| Spoofing 仿冒 | 他人向机器人发指令操控电脑 | IM 指令白名单（仅配置的 user_id）；通道级鉴权 | 白名单外用户发指令→audit 记录 denied |
| Tampering 篡改 | 市场技能被植入恶意代码 | Skill Scanner 静态扫（密钥/eval/exec/反连/提权）+ 人工抽检 + 技能沙箱 | 恶意样本测试集（预埋 20 个坏技能）全部拦截 |
| Repudiation 抵赖 | 技能执行了危险操作无凭据 | audit.db 只追加全留痕 + 操作哈希链 | 篡改检测：重算哈希链断裂即告警 |
| InfoLeak 泄漏 | 敏感内容上云（截图/凭据） | never_send 黑名单 + 发送前 NER 二次扫描 + 脱敏 | 隐私断言测试（D.1）+ 人工红队 50 样本 |
| DoS 拒绝 | 智谱 429/断网导致系统假死 | pacer+队列降级+本地兜底 | 异常注入（D.6） |
| Elevation 提权 | 技能脚本越权写系统目录 | 沙箱白名单目录 + Tool Guard 二次确认 | 沙箱逃逸用例集（预埋 10 个逃逸尝试） |

## F.3 微信纯视觉四铁律的威胁面复核
攻击面分析：不注入→不触发微信进程完整性校验；不发送→不产生非人类行为指纹；原文不存→即使本机被入侵也无微信明文库；默认关→默认攻击面为零。残余风险仅"截图中含敏感信息被 VLM 处理"——黑名单+裁剪+脱敏三层缓解，且 VLM 响应不回传原文（只存摘要）。

## F.4 应急响应流程（SOP）
1. 发现（audit 告警/用户报告）→ 冻结：暂停感知与通道（托盘一键）；
2. 取证：audit.db + 哈希链导出（只读副本）；
3. 止损：撤销通道令牌、Skill 下架/ban、必要时全网市场熔断（index 仓库 revert）；
4. 通告：24h 内 GitHub 安全公告 + 企微用户通告（模板预写）；
5. 复盘：5Whys 入 PDCA，威胁矩阵更新（F.2 为活文档）。

## F.5 合规映射
PIPL：本地处理为原则、上云内容脱敏、用户可导出可删除（数据主权三件套）；AIGC 管理办法：生成内容显式标识"由 PAI-Station 生成"；未成年人/特殊数据：不采集（黑名单兜底）。企业版附加：算法备案指引、数据出境评估材料清单。


---


# 附录 G 术语表与常见问题

## G.1 术语表（Ubiquitous Language，全项目统一用词）

| 术语 | 定义 | 禁用同义词 |
|---|---|---|
| 感知 | L1 层采集原始信号的动作 | 监控、窥探 |
| 记忆 | memory.db 中带出处的可检索条目 | 数据、日志 |
| 蒸馏 | 从初终稿 diff 抽取方法论规则的过程 | 训练、学习（模型训练义） |
| 技能 | SKILL.md 规范的可执行方法论单元 | 插件、脚本（狭义） |
| 抽屉 | 零打扰成果暂存区 | 收件箱、通知中心 |
| 采纳率 | 初稿与终稿编辑距离的倒数指标 | 准确率 |
| 分身资产 | avatar.pai（画像+技能+记忆索引包） | 备份、账号 |
| 铁律 | 代码硬编码不可配置的安全约束 | 规则（普通义） |
| 系统1/系统2 | fast/deep 模型通道（Kahneman） | 快慢模式 |

## G.2 FAQ（按用户视角回答）

**Q1 它会不会偷看我的隐私？**
感知默认只覆盖你勾选的目录；黑名单窗口永不感知；一切动作 audit 可查；托盘一键全局暂停。隐私报告每月推送："这个月看了什么、存了什么、什么发了云端"。

**Q2 微信会不会被封号？**
微信通道零注入零发送（四铁律），只做屏幕视觉识别且默认关闭——与"真人看屏幕"等价，不触碰任何微信检测面。

**Q3 真的一分钱不花？**
智谱免费链覆盖核心功能；重度用户超免费额度才需要 Pro ¥19/月，且免费档永不阉割核心功能。

**Q4 和 ChatGPT/豆包有什么区别？**
它们是你去找的聊天工具；PAI-Station 是住在电脑里、看着你工作、随时间越来越懂你的工作站——被动问答 vs 主动复利资产。

**Q5 数据换电脑怎么办？**
avatar.pai 一键导出导入，能力零损失（迁移测试验证）。

**Q6 技能市场的东西可信吗？**
每个技能过静态扫描+人工抽检；采纳率标签公开；退款判据用采纳率数据仲裁。

**Q7 我是程序员，能贡献什么？**
适配器（组件层）、通道（继承 Channel 四方法）、技能蒸馏案例、安全测试——贡献指南见 README。

## G.3 与既有成熟项目的亲缘关系一览
We-AIPO→限速重试代码；RE-Factory→规则引擎范式；CPOPC 31 项目→办公场景语料与痛点库；E:\AIResearch→调研方法。PAI-Station 站在这一切之上（含 350 项开源调研），这也是"站在巨人肩膀"原则的项目内表达。


---


# 附录 H 六大引擎详设（类设计、核心伪代码与时序）

## H.1 感知引擎（sense/）

```python
class SenseBus:                       # 事件总线：统一各源输出
    def publish(self, event: SenseEvent): ...   # 观察者分发给 memory/learn

class SenseEvent:
    type: str; ts: int; payload: dict
    # doc: {path, ext, size, mtime}   screen: {img_path, fg_app, fg_title}
    # im_*: {channel, chat, sender, text}   commit: {repo, sha, files}

class FSWatcher:                      # watchdog 封装
    def on_any_event(self, e): self._debounce(e)      # 3s 窗口合并
    def _flush(self, batch): SenseBus.publish(doc_batch=batch)

class ScreenLoop:                     # mss 线程
    def tick(self):
        if self._blacklisted(fg_title): return self._skip_and_log()
        img = mss.grab(virtual_screen); save_rolling(img, ttl=600)
        event = vision.describe(img) or ocr_local(img)   # 云优先本地兜底
        self._dedup_by_hash(event)                        # 相似截图（<0.1 diff）不重复上报
```
时序：watchdog 事件→去抖→MarkItDown 队列（进程池 2）→SenseEvent→memory.ingest。全链路背压：队列满时丢 screen 保 doc。

## H.2 上下文引擎（memory/）

```python
def capture(ev) -> str:               # ingest 主函数
    md = to_markdown(ev)              # MarkItDown / MinerU / vision json
    facts = fast(f"{PROMPT_SUMMARIZE}\n{md[:6000]}")     # 200字摘要+实体 json_mode
    score = future_recall_prob(facts)  # 写入端审计（2.3 创新点）：
        # score = f(type权重, 实体热度, 情绪信号, 与既有记忆链接数)
    mem = Memory(...); store.insert(mem); entity_map.upsert(facts.entities)
    viking.sync(mem) if score > 0.7 else viking.index_only(mem)
    return mem.id

def recall(q, top_k=8) -> list:       # 三路召回+RRF 融合
    r1 = fts5.search(jieba(q))                       # 关键词
    r2 = entity_graph.expand(entities(q), hops=2)    # 实体图
    r3 = timeline.filter(parse_dates(q), window)     # 时间线
    fused = rrf([r1, r2, r3]); boost_by_weight(fused)
    return fused[:top_k]
```

## H.3 规则引擎（rules/）
规则表示统一为 `(条件模式, 动作, 来源, 置信度, 样本数)`；加载时跑冲突检测（同条件模式不同动作→按置信度+样本排序，冲突标记进 PDCA）。个人规则蒸馏与技能蒸馏共用 distiller 内核（修改块聚类），差别仅在输出目标（rules.ini vs SKILL.md）。

## H.4 技能引擎（skills/）
执行时序：指令/触发→匹配技能（triggers 声明）→File Guard 按 privacy 授权→组装上下文（SKILL.md 规则+recall 结果）→按 level 路由模型→生成→落盘抽屉→监听编辑（watchdog 盯初稿文件）→diff→distiller.feedback(adopted_diff)→采纳率滚动更新→状态机流转（A.4）。

## H.5 主动引擎（proactive/）
```python
def on_trigger(t):                    # APScheduler 回调
    task = infer_task(t)              # 系统1分类：这是什么活
    M = jtbd_score(task)              # 功能+情感+社会 × 紧迫
    A = skill_readiness(task)         # 有技能且 stable？记忆召回够？
    P = attention_ok() and not quiet_hours()
    B = M * A * P
    quadrant = (importance(task), urgency(t))
    action = dispatch(B, quadrant)    # 1.5③ 分流表
    if action in (push, drawer): executor.run_async(task, action)
    if action == push: budget.spend() # 打扰预算扣减，超支自动降 drawer
```

## H.6 自学习引擎（learn/）
每日 02:00：①KPI 盘点（采纳率/召回率/拒绝率/失败任务）；②对每个失败任务 whys.run（z1 + 日志证据链，最多 5 层）→ fishbone 归类；③单环项直接改（参数/提示词版本），双环项生成提案→企微审批卡片；④所有改动写 changelog（git 化 rules/skills）→ audit 留痕。月度：Ebbinghaus 权重重算、记忆链接清洗、画像重算（季度全量）。


---


# 附录 I 首年运营手册（内容、渠道、社区 SOP）

## I.1 内容日历（发布前 4 周预热 + 全年主线）

| 周 | 主内容 | 渠道 | 目的 |
|---|---|---|---|
| 发布-4 | 《350 项个人 AI 工作站开源组件选型报告》 | 知乎长文+GitHub | 立专业人设，SEO 引流 |
| 发布-3 | 《我解剖了 4 个 40k star 竞品，发现一个生态空白》 | 掘金/V2EX/即刻 | 争议性流量 |
| 发布-2 | 《让电脑记住你的一切：PAI 感知引擎设计》 | 公众号+博客 | 技术信任 |
| 发布-1 | 《29 个思维模型如何变成代码》 | 知乎 | 方法论差异化 |
| 发布日 | GitHub 公开 + Product Hunt + 中文社区矩阵 | 全渠道 | 爆发 |
| +2 周 | 首批用户案例（周报省时实测数据） | 短视频+图文 | 转化 |
| 月度节奏 | 每月 2 篇深度 + 4 条技巧 + 1 次版本发布 | 固定 | 心智占位 |

## I.2 渠道打法与预期（数字可核对）
- Product Hunt：openhuman 路径验证（其获 #1 周榜）；我们主打 China-first 差异点；
- 知乎/V2EX/掘金：技术长文导流（转化率行业均值 3-8% 安装）；
- 即刻/小红书：职场效率人群（周报/公文技能切中公务员与白领痛点）；
- B 站/抖音：3 分钟实机演示（主动服务最直观）；
- GitHub trending：README 对比表+一键安装是转化关键；
- 预期漏斗：首月曝光 50 万 → GitHub 访问 5 万 → 安装 5,000（10%）→ 周活 2,500（50%）。

## I.3 社区运营 SOP
- issue 分诊 SLA：24h 响应；bug/feature/discussion 三标签；
- 贡献者阶梯：装了用→提 issue→交 PR（适配器/技能/通道三赛道）→committer（季度评审）；
- 技能作者社群：种子 100 人微信群，月度采纳率榜单+现金奖；
- 危机预案：安全事件走 F.4 SOP；负面舆情 2h 内技术回应（用 audit 数据说话）。

## I.4 指标看板（每周一晨会读数）
北极星：周活分身产生的工作小时节省数（= Σ采纳成果×人工耗时估计）。
辅助：安装→首周留存（>60%）、技能市场供需比、Pro 转化、NPS（季度）、CAC（渠道分摊）。
反指标（越小越好）：打扰拒绝率、崩溃率、隐私投诉数（目标零）。


---


# 附录 J 开发环境搭建与 AI 协作手册（新人 30 分钟上手）

## J.1 环境搭建（Windows 10/11）
```
1. Python 3.11.9（winget install Python.Python.3.11）
2. git clone https://github.com/huangxiding-creator/PAI-Station && cd PAI-Station
3. python -m venv .venv && .venv\Scripts\activate
4. pip install -r requirements.txt   # 19 依赖见 J.2
5. copy config\pai.example.ini config\pai.ini  # 填智谱 key（免费注册 open.bigmodel.cn）
6. python -m paistation --check       # 配置自检+模型连通测试
7. python -m paistation               # 托盘启动；pytest -q 全绿后开发
```

## J.2 依赖清单（全部主流维护，锁版本发布）
lark-oapi、dingtalk-stream、DrissionPage、watchdog、mss、dxcam（可选）、APScheduler、tenacity、python-docx、pywebview、pystray、paddleocr（或 rapidocr 轻量版）、funasr+torch（SenseVoice，可选重依赖）、jieba、jsonschema、pyinstaller、WinSW（发行随包）。

## J.3 工程规范
- 分支：main 保护+feat/fix 短分支；PR 必须过 CI（lint+单测+隐私断言）；
- 提交：conventional commits（feat/fix/docs/chore）；每次提交附验收锚点编号（如 M1.8）；
- 文档即代码：PROPOSAL_V2 章节与代码模块一一对应，改动代码若影响设计文档必须同步 PR；
- 测试金字塔见 D.1，覆盖率门禁 80%。

## J.4 与 AI 结对开发流程（本项目的生产方式）
用户主导的方向决策 + AI 执行的工程循环：任务卡（验收锚点）→AI 实现→测试自证→code review→合并。此模式已在 We-AIPO 验证（290 commits/78 项优化/11/11 无人值守达标）。规范：AI 产出必须带测试与 audit 覆盖；禁止无验收标准的"顺手重构"。

## J.5 发布流水线
pytest→pyinstaller 打包→签名→WinSW 服务安装脚本→内测通道（20 人）→GitHub Release（附 SHA256）→目录站同步。


---


# 附录 K 架构决策记录（ADR：为什么选 X 不选 Y——15 条存档）

| ADR# | 决策 | 被否方案 | 决定理由 |
|---|---|---|---|
| 01 | SQLite 单文件四库 | PostgreSQL/MongoDB | 零运维、便携、备份=拷文件；量级未到 |
| 02 | 智谱免费链 | 付费 API/本地模型 | 零成本约束+128K+30 并发唯一解；隔离层留退路 |
| 03 | 微信纯视觉 | Hook/协议逆向 | 封号风险一票否决；零注入=零检测面 |
| 04 | OpenViking sidecar | 直接 import | AGPL 传染；进程边界=许可证防火墙 |
| 05 | SKILL.md 兼容 anthropics | 自定义 DSL | 最大技能生态互通，市场冷启动借势 |
| 06 | 技能=Git 仓库 | 平台托管 | 零服务器成本+天然版本化+抗单点 |
| 07 | 事件驱动+20min 节拍 | 纯轮询 | CPU 预算 2% 红线 |
| 08 | 抽屉默认不弹窗 | 全部即时通知 | 打扰是主动服务的第一死因 |
| 09 | FTS5+jieba 先行 | 向量库一步到位 | 中文分词可解释、免嵌入成本；50 万条再混合 |
| 10 | INI 而非 YAML/JSON | - | 用户硬性约定+手改友好 |
| 11 | audit 只追加 | 可编辑日志 | 审计可信性前提 |
| 12 | 单进程多线程 | 微服务 | 单机单用户，通信开销无谓 |
| 13 | WinSW 服务化 | 计划任务 | 崩溃守护+开机自启一体化 |
| 14 | z1 推理链用于蒸馏/根因 | 全用 fast | 规则抽取质量是 P2 命门，值得慢 |
| 15 | 双环改动须审批 | 全自动自改 | 用户信任优先于自动化程度 |


---


# 附录 L 提示词资产库（prompts/ 目录规范与首批 12 条全文）

> 提示词=产品资产（E.5）：文件化、版本化、基准回归。此处收录首批 12 条核心提示词全文要点，开发时直接扩充成文件。

| 文件 | 用途 | 核心结构（system 要点） |
|---|---|---|
| summarize_v1.md | 文档摘要（L1→L2） | 角色：档案员。输出 JSON{summary≤200字, entities[], tags[], date_refs[]}。禁评论禁推断，只述事实。 |
| vision_screen_v1.md | 截图结构化 | 输出 JSON{app, window_title, text_summary, action_type, entities[]}；不确定性写 unknown 禁编造。 |
| extract_rules_v3.md | 蒸馏规则抽取（z1） | 输入：diff 修改块×N+任务上下文。规则形式"当<条件>时<动作>"，每条附频次证据；合并同型；输出 SKILL.md 规则段草稿。 |
| jtbd_score_v1.md | 主动动机打分 | 功能/情感/社会各 1-5 分+一句理由；紧迫度由时间窗口计算（代码侧），LLM 只出三分项。 |
| whys_v2.md | 5Whys 根因（z1） | 每层 Why 必须引用日志行号/audit id；最多 5 层；输出根因分类∈{数据,规则,技能,模型,通道}。 |
| profile_bigfive_v1.md | 画像打分（z1） | 每维度 1-5 分+证据链（引用 memory_id）；低于阈值输出"未知"。禁绝对化标签。 |
| quadrant_v1.md | 象限判定 | importance（对长期 JTBD 贡献）/urgency（窗口宽度）各 1-5；输出象限号+理由≤20字。 |
| outline_minto_v1.md | 金字塔大纲 | 先结论句；三层以内；同层 MECE 自检；每节标证据需求（哪些记忆待召回）。 |
| scqa_push_v1.md | 推送文案 | S-C-Q-A 四句式；总长≤80字；附出处与"是否需要我处理"问句。 |
| contract_scan_v2.md | 合同九区扫描 | 逐区输出{条款引用,风险级,修改建议,法律依据}；缺失项单列；长文自动分段。 |
| confirm_double_loop_v1.md | 双环审批卡片 | 变更前后对比+风险+回滚方案，≤120字，附"同意/拒绝"指令说明。 |
| skill_card_v1.md | 市场技能卡片 | name+一句话价值+采纳率+样本数+适用场景三条；禁夸张词（最好/第一）。 |

版本规则：语义化 v{n}；改提示词必跑关联基准（summarize→500 事实集；vision→200 标注集；extract_rules→蒸馏消融）；基准脚本输出对比表入 PR 描述。


---


# 附录 M 首批 100 个种子技能清单（M5.1 的备货单）

> 六大家族×~17 变体。命名=市场检索键；标 ★ 为首发主推（预计需求最大）。

**公文家族（18）**：★红头通知 / ★红头请示 / ★红头报告 / 公函 / 会议纪要-政务 / ★工作总结-季度 / 述职报告 / 民主生活会发言 / 信息简报 / 调研报告 / 督查专报 / 领导讲话稿-会议 / 领导讲话稿-调研 / 主持词 / 表态发言 / 提案-政协 / 建议答复 / 年终盘点。

**周报月报家族（16）**：★互联网周报 / ★制造企业周报 / 销售周报 / 研发周报 / 项目双周报 / 跨境运营周报 / 教师周记 / 医疗科室月报 / 财务月度分析 / HR 月报 / 市场月报 / 供应链月报 / 客服月报 / 政务月结 / 设计团队月报 / 自媒体数据周报。

**会议家族（15）**：★飞书会议纪要 / ★腾讯会议纪要 / 线下例会纪要 / 1v1 记录 / 头脑风暴整理 / 复盘会-AAR / 立项评审 / 招聘面试记录 / 客户拜访记录 / 供应商谈判要点 / 项目周例会 / 全员大会要点 / 政府座谈会纪要 / 业主沟通记录 / 跨部门协调会。

**合同法务家族（15）**：★劳动合同审查 / ★采购合同审查 / 销售合同审查 / 租赁合同审查 / 保密协议 / 服务协议 / 合作框架协议 / 竞业限制审查 / 离职文件包 / 催款函 / 律师函草拟 / 商务道歉函 / 知识产权许可 / 股权代持风险扫描 / 合同台账登记。

**数据报表家族（18）**：★销售日报 / ★经营周报看板 / 库存周报 / 电商大促复盘 / 广告投放周报 / 财务对账单 / 项目成本跟踪 / KPI 达成表 / 招聘漏斗周报 / 客户成功健康度 / 工时统计表 / 采购比价表 / 物业收费月报 / 教学进度表 / 病区床位日报 / 舆情监测摘要 / 竞品价格跟踪 / 供应商绩效评分。

**个人效能家族（18）**：★OKR 季度制定 / ★年度总结-个人 / 简历优化 / 求职信 / 转正申请 / 调薪申请 / 离职申请 / 请假条-得体版 / 邮件改写-委婉 / 邮件改写-催办 / 朋友圈文案 / 小红书标题 / 公众号大纲 / 读书笔记-卡片 / 学习计划制定 / 旅行攻略 / 家庭财务月结 / 礼物清单策划。

**备货计划**：团队自用蒸馏 60 个（我们真实工作产生）+ 种子作者共创 40 个；每个技能上架需：scanner 通过 / 采纳率≥50% 或标注"学习期" / 三联证据包（附录 A.4 状态机）齐全。


---


# 附录 N 安装排查与升级 FAQ（客服底稿）

| 症状 | 根因 | 处置 |
|---|---|---|
| 启动报"缺少 [llm].api_key" | 未复制 example 配置 | copy config\pai.example.ini config\pai.ini 后填 key |
| 模型连通测试 401 | key 错误/未激活 | 到 open.bigmodel.cn 控制台核对；新号需实名 |
| 托盘图标不见 | 安装时选了静默、explorer 重启 | 重跑 WinSW 服务注册；任务栏设置显示图标 |
| 飞书收不到消息 | 事件订阅未选长连接/权限未勾 | 按 5.2 步骤 3 重配；--check 输出缺失权限清单 |
| 企微推送失败 | 应用可见范围不含自己 | work 后台把用户加入可见范围 |
| 截图识别全是 unknown | 断网/4v 限流 | 自动降级 PaddleOCR；检查 pacer 日志 |
| CPU 飙高 | watch_dirs 含了工程目录（node_modules 风暴） | ignore_patterns 加 node_modules,.git |
| 记忆查询很慢 | 库>10 万条 | 检查 FTS5 索引；触发 OPTIMIZE；viking 分层生效确认 |
| 微信识别不到内容 | 微信窗口标题变化/黑名单误伤 | 查 screen_blacklist；窗口标题匹配规则热修 |
| 升级后 schema 报错 | 迁移被中断 | 自动回滚备份库；日志查 migrations/ 卡点 |

**升级策略**：应用内检查更新→下载差分包→校验 SHA256→备份四库→应用迁移→回归冒烟（500 基准抽 50 条）→回滚开关。升级失败自动恢复旧版本并保留错误报告入口。


---


# 附录 O 贡献者指南与技能作者分成细则（生态契约全文）

## O.1 贡献者四赛道
1. **适配器赛道**：为组件生态新增适配器（19 件清单外的新开源件）。要求：实现 base 接口+验收锚点+README。署名进组件总表。
2. **通道赛道**：继承 Channel 四方法（5.6）。要求：断线重连测试+权限分级+文档。当前需求池：QQ 官方机器人、Telegram、Slack。
3. **技能赛道**：蒸馏真实技能并上架。要求：三联证据包+采纳率标注+隐私分级声明。
4. **安全赛道**：威胁样本（坏技能/逃逸用例）、渗透报告。奖励：安全致谢榜+市场信用分。

## O.2 技能市场契约（作者版）
- **定价权**：作者自主 0-99 元；平台抽成 15%（种子作者首年免佣）；
- **结算**：月度自动打款（微信/支付宝），满 ¥100 起结；
- **退款**：买家 7 天内可退；仲裁依据=技能标注的采纳率与买家实测初稿采纳率偏差（Kano 判据，7.2 节）；恶意退款（下载即退）风控拦截；
- **下架**：投诉成立/scanner 新规则追溯命中/连续两月采纳率 <30% 且差评 >20%——下架不删库（买家已购可继续用）；
- **原创保护**：技能文本相似度检测（SimHash>0.85 判重复）拒绝抢注；举报通道 48h 响应。

## O.3 市场信用分体系
作者信用分 0-100：初始 70；上架过审+2；好评+1；差评-2；退款率 >10% 每月 -5；scanner 追溯违规 -30；安全贡献 +10。低于 50 禁止上新。买家侧同样有信用（恶意退款影响仲裁权重）。

## O.4 法律与合规边界（写在契约里）
- 技能不得包含：破解/外挂/批量营销骚扰/个人信息采集类功能（scanner 关键词+人工双审）；
- 数据授权：技能执行中接触的用户数据归用户，作者对技能运行结果无任何权利主张；
- 免责：技能输出为初稿建议，重大决策（法律/医疗/财务）用户自负审核责任——技能描述页强制展示。

## O.5 社区治理
季度公开治理报告（上/下架数、退款率、申诉处理时效）；治理规则变更走 RFC（社区提案→7 天公示→生效）。**生态契约的可信度就是市场的地基**——契约本身也是产品。


---


# 附录 P M0 第一周逐日演练（新人照抄版——"不懂的人也能上手"的样板）

> 目的：把第 8.2 节的任务卡展开成逐日操作实录模板。第一天从零到"托盘图标亮起"，每周五验收锚点全绿。AI 结对协作按附录 J.4 流程执行。

**周一·骨架日**
上午：按 J.1 装环境，跑通 `python -m paistation --check`（此时只校验 INI）。下午：建 4.2 目录树空文件；写 pyproject（依赖仅 watchdog/APScheduler/tenacity 三件起步）；配 ruff+pytest；首个提交 `chore: M0.1 skeleton`。验收：CI 绿。

**周二·配置日**
实现 config.py：ini 解析→校验→中文报错（错误类型对照附录 C.4 五类）。写 20 个校验单测（含非法值矩阵）。验收锚点 0.2：故意填错 5 个字段，报错逐条准确。

**周三·模型日**
移植 We-AIPO zhipu.py：先原样拷贝再适配三方法；写 mock 测试（不打真 API）+1 条真连通透测（--check 里）。验收锚点 0.3：fast/deep/vision 各跑一次真实调用返回结构正确。

**周四·路由与审计日**
上午 router.py：置信度升级逻辑+单测（0.69/0.71 边界）。下午 audit.py+file_guard.py：全动作 decorator 落库；file_guard 白名单拦截测试。验收锚点 0.5：任何文件读动作在 audit 可查，越界读被拒并留痕。

**周五·服务与托盘日**
WinSW 注册服务（开机自启+守护）；pystray 托盘（暂停感知/退出/设置三个菜单）；模拟断电重启 3 次。验收锚点 0.6/0.7。周复盘（PDCA 首圈）：走查五天产出，未竟项入下周。

**第一周出口标准（全部可证伪）**：12+ 测试全绿；五项验收锚点各有一段 30 秒演示录屏；git 历史干净（每任务一提交，附锚点号）。**这就是全项目的节奏样板**：任务卡→实现→测试→录屏→提交，M1-M5 重复此循环 22 周。


---


# 附录 P2 M1-M5 逐周演练（延续附录 P 的样板到全部里程碑）

## M1 感知与记忆（第 3-6 周）
**第 3 周（感知骨架）**：周一 FSWatcher+去抖单测；周二 MarkItDown 集成+Office 三件套转换测试（95% 锚点脚本）；周三 MinerU 兜底链+失败降级日志；周四 screen.py 黑名单+滚动缓冲；周五 4K 截图性能锚点+周复盘。**第 4 周（视觉理解）**：周一 GLM-4V 调通+schema 约束；周二-周三 200 张标注集构建（自家屏幕抽样 150+公测图 50，标注 app/标题/文本摘要）；周四 识别准确率跑分迭代提示词至 ≥90%；周五 断网 OCR 降级+dedup 哈希。**第 5 周（记忆落库）**：DDL 建库+FTS5 触发器；ingest 摘要流水线（summarize_v1）；写入端打分器；500 事实集启动构建（从本周真实工作文档抽 200 条起步）。**第 6 周（召回与验收）**：三路召回+RRF；实体图两跳；LoCoMo 子集+500 基准双跑；隐私断言套件；M1 出口评审（录屏：新文件→90 秒内可检索）。

## M2 通道（第 7-9 周）
**第 7 周（抽象与飞书）**：周一 Channel 基类+指令路由骨架；周二-周四 飞书 WS 全链路（含文件收发与卡片回复）；周五 验收演示（群 @ 机器人 60 秒应答）。**第 8 周（企微与钉钉）**：企微轮询+主动推送+审批卡片（4 天）；钉钉 stream（2 天）；断线重连演练（拔网 30 秒）。**第 9 周（微信视觉与收口）**：wechat_vision 四铁律硬编码+首启确认流（3 天）；通道权限分级测试（2 天）；30 天挂机监测挂上（后台跑，M5 前看结果）。

## M3 技能与主动（第 10-14 周）
**第 10 周（执行器）**：SKILL.md 解析+触发器注册+File Guard 按 privacy 授权；首个 builtin 技能（周报 v0 手写规则）端到端跑通。**第 11-12 周（蒸馏器——核心两周）**：SequenceWrapper 修改块对齐→同型聚类→z1 规则抽取（extract_rules_v3）→SKILL.md 生成；三联证据包落盘；消融协议脚本（对照组/实验组各 20 任务）开跑。**第 13 周（主动引擎）**：四类触发器；attention 状态机（deepwork/quiet_hours）；Fogg 打分+Eisenhower 分流；打扰预算扣减。**第 14 周（闭环与验收）**：outbox 抽屉 UI+托盘轻闪；拒绝率反馈降权；两周 A/B 数据出报告（每周 ≥3 采纳、打扰 ≤2/天、消融 ≥25pp——三个锚点一次过）。

## M4 画像与自学习（第 15-18 周）
**第 15 周（个人规则）**：蒸馏共用内核改输出目标 rules.ini；MECE 冲突检测器。**第 16 周（人际与企业画像）**：contacts 三件套（BigFive 证据链+MBTI 速写+关系情境）；org 四框架模板+GB/T 9704 版式库（python-docx 十七项自检）。**第 17 周（自学习）**：PDCA 每日 02:00 作业；whys_v2+日志证据约束；fishbone 归类。**第 18 周（双环与收口）**：双环提案→企微审批卡片→回执执行链路；画像季度校准流程；5Whys 根因抽检 ≥80% 锚点。

## M5 生态发布（第 19-22 周）
**第 19 周（打包）**：pyinstaller+签名+WinSW 安装脚本；卸载残留测试。**第 20-21 周（市场备货）**：自用蒸馏 60 技能+种子作者共创 40（附录 M 清单）；scanner 上架闸+人工抽检；index 仓库与静态目录站。**第 22 周（发布）**：README 终稿+对比表；内测 20 人一周（每日回收问题分级）；渗透测试跑附录 F 场景；GitHub Release+渠道矩阵首发（附录 I 内容日历的发布日）；当晚看板读数定下一步。


---


# 附录 Q 十大尖锐问题正面回应（投资人与用户的必问清单）

**Q1 微信方案是不是灰色的？将来被打击怎么办？**
纯视觉只读在技术事实层面与"人看屏幕"等价：不注入进程、不模拟输入、不调用任何非公开接口、不发送任何内容——不落入微信外挂判定要件。残余风险（截图含隐私被 VLM 处理）用黑名单+裁剪+脱敏+默认关闭四层压住，且通道整体可一键下线而不影响其余功能（R2 对策）。我们从不承诺"永久"，我们承诺"每一步都有退路"。

**Q2 免费模型链是商业模式的地基，智谱改政策怎么办？**
三重对冲：适配器三行切端点（技术）；Pro 档含付费通道预算（商业）；蒸馏出的技能与记忆资产不受模型影响（资产）。最坏情形：感知降频、能力降档，产品不死——因为价值主体在资产层不在模型层。

**Q3 大厂（阿里/字节/腾讯）下场复制怎么办？**
六维矩阵（6.5 节）显示结构性差异而非功能差异：大厂做个人工作站必然导向自家云（组织惯性），而我们的价值主张恰是"资产在本地"——这是大厂最难复制的立场（与自身利益冲突）。速度+开源社区+技能双边网络是先发护城河；蒸馏方法申请专利（R5）。

**Q4 用户真的会为"初稿"付钱吗？**
采纳率数据是关键：消融协议（D.4）要求蒸馏带来 ≥25 个百分点采纳率提升——初稿达到"改三处就能交"的质量时，用户买的是每周 3 小时而不是"AI 生成物"。若数据不达标，我们不上 Pro 定价——指标先于商业承诺。

**Q5 会不会产生幻觉把错误写进记忆/技能？**
记忆层：摘要只述不推（提示词约束）+出处强制+置信度分级；技能层：规则必须附频次证据，<5 样本标"学习期"不可上架；输出层：所有对外成果标"初稿建议"。自学习层把幻觉事故当一类根因单独统计（石川图第六类新增"幻觉"）。

**Q6 数据全在本地，坏了怎么办？**
每日 03:00 自动备份（30 天滚动）+avatar.pai 手动导出+云备份是 Pro 权益——数据主权与数据安全不冲突，选择权在用户。

**Q7 技能市场会不会变成垃圾场？**
三道闸：scanner 机器审+采纳率数据标签+信用分体系（O.3）；下架不删库保护买家。GPT Store 的混乱恰是"无数据信任标签"的结果——采纳率标签是我们对整个行业的差异化回答。

**Q8 你们只有一个人，做得完吗？**
22 周计划按"1 人+AI 结对"编排（We-AIPO 已验证该模式 290 commits 产出）；四自研模块合计 2,500 行为核心资产，其余缝合。风险 R8 的对策是任务全文档化——本手册本身就是"第二个人随时可接手"的证明。

**Q9 隐私事故的兜底是什么？**
事前：五层安全+四铁律+never_send；事中：audit 全留痕+Tool Guard 确认；事后：F.4 应急 SOP（冻结-取证-止损-通告-复盘），24 小时通告承诺写进治理文档。

**Q10 为什么不是开源核心+商业版（open-core）？**
MIT 全开源+生态变现：个人市场需要信任最大化，开源核心版会永久质疑"免费版是不是残废的"；我们的免费版永不阉割（Kano 基本型保底），变现全部来自生态（市场/算力/企业）——与 openhuman 的 GPL+订阅、QwenPaw 的云导流都不同，这是站在 350 项调研肩膀上选出的第三条路。


---


# 附录 R 市场测算推导细节（第 9 章每个数字的公式与数据源）

## R.1 知识工作者 4 亿的推导
国家统计局 2024 就业人员 7.34 亿；按《中国统计年鉴》行业构成，信息技术（~700 万）、金融（~1,200 万）、公共管理与社会组织（~2,000 万）、教育（~1,800 万）、卫生（~1,400 万）、专业技术服务与租赁商务（~4,000 万）、文体娱与科研（~800 万）等白领岗位合计约 3.8-4.2 亿——取保守 4 亿。交叉验证：麦肯锡 2023 报告中国"数字化知识工作者"约 3.6 亿（口径略窄），两个独立来源同量级，可信。

## R.2 ARPU ¥84/年的推导
Pro ¥19/月×付费率 8%×12 = ¥18.3；技能市场人均年消费 ¥30×参与率 15%×抽成 15% 折算到平台 = ¥0.68/人；企业版摊薄 ¥1.2/人（50 家×47 万÷400 万）；合计约 ¥20/人/年——等一下，第 9 章 ¥84 是"客单价口径"（用户侧价值）非平台收入口径：用户侧 = Pro 付费者 ¥228 + 市场消费者 ¥45 + 免费用户 ¥0 加权 = ¥84/活跃用户潜在年支出。平台收入侧见 7.6（第 5 年 ¥6-8 亿对应平台口径 ARPU ¥15-20）——两个口径分开表述，避免"用户支出=平台收入"的常见夸大错误。

## R.3 渗透率 1% 的对标锚
Notion 全球 5 年从 0 到 3000 万注册（渗透全球知识工作者 3%，但注册≠活跃）；国内滴答清单 10 年 5,000 万注册、活跃约 1,000 万（渗透中国知识工作者 2.5%）；Flomo 5 年 300 万。我们取 1% DAU（400 万）= 滴答清单活跃量的 40%，属于"成功但非爆红"档——基准档的保守性即在于此。

## R.4 病毒系数 0.3-0.8 的构成
分身年度报告（年 1.2 次分享×15% 注册转化×70% 留存=0.13）+技能分享（年 2 次×8%×30%=0.05）+内容渠道（非病毒，不计入 K）→ 有机 K≈0.18/年即月 0.015——单独看很低；但内容获客是主力（月 5,000 安装），K 因子是放大器不是发动机。7.2 节"生态飞轮"的真实含义：技能越多内容越多（每个上架技能都是一篇潜在素材）→ 获客成本随技能库增长递减——这是与"买量模式"的根本区别。

## R.5 收入三情景的蒙特卡洛直觉
把 DAU、付费率、ARPU、市场 GMV 四变量按悲观/基准/乐观三角分布抽样 10 万次：第 5 年收入 P10≈¥1.2 亿、P50≈¥5.5 亿、P90≈¥13 亿——7.6 表"基准 ¥6-8 亿"对应 P50-P60 区间，"保守 ¥1 亿"对应 P10，"乐观 ¥20 亿+"对应 P95 之外（需出海成功）。敏感度：DAU 弹性 1.5 最大（双边网络），付费率弹性 0.8，价格弹性 -0.3——结论：一切资源优先投获客与留存，而非提价。

## R.6 反面清单（我们不做但读者会联想到的测算）
- 不算"替代 ChatGPT 会员"的节省叙事（定位不同：工作站 vs 聊天工具）；
- 不算企业降本的效率换算（第 9.4 节只做定性——效率节省让用户续费，不是我们向企业收钱的依据）；
- 不预测政策变量（R9 风险登记而非收入情景）。
**测算纪律**：每个对外引用的数字必须能回溯到本附录的公式与来源；不能回溯的数字不进对外材料。


---


# 附录 S 核心模块参考实现（M0 三件套可运行代码——真实起点，非伪代码）

> 本附录给出 M0 三个核心文件的参考实现（约 300 行，可直接作为开发起点）。风格遵循项目规范：小函数、显式错误、全中文报错、无裸 print。

## S.1 config.py（INI 加载与校验，节选）

```python
"""配置加载器：INI -> 强类型 Config 对象，非法值中文报错。"""
from __future__ import annotations
import os
from configparser import ConfigParser
from dataclasses import dataclass
from pathlib import Path

class ConfigError(Exception):
    """配置错误，message 直接可展示给用户。"""

@dataclass(frozen=True)
class PAIConfig:
    llm: "LLMConfig"   # 其余各段同理展开

_RANGE = {"upgrade_confidence": (0.5, 0.9)}

def _get(parser: ConfigParser, section: str, key: str, default: str) -> str:
    if not parser.has_section(section):
        raise ConfigError("缺少配置段 [%s]，请对照附录 C.1 补全" % section)
    val = parser.get(section, key, fallback=default)
    if not val:
        raise ConfigError("[%s].%s 为空：该字段无默认值，必须填写" % (section, key))
    return val

def _float(section: str, key: str, raw: str) -> float:
    try:
        num = float(raw)
    except ValueError as err:
        raise ConfigError("值 '%s' 不是合法数字（[%s].%s）" % (raw, section, key)) from err
    lo, hi = _RANGE.get(key, (None, None))
    if lo is not None and not lo <= num <= hi:
        raise ConfigError("%s 超出 [%s].%s 允许范围 %s-%s" % (num, section, key, lo, hi))
    return num

def load(path="config/pai.ini") -> PAIConfig:
    file = Path(path).expanduser()
    if not file.exists():
        raise ConfigError("找不到配置文件 %s：请复制 config/pai.example.ini 为 pai.ini 后填写" % file)
    parser = ConfigParser()
    parser.read(file, encoding="utf-8")
    api_key = os.environ.get("PAI_LLM_KEY") or _get(parser, "llm", "api_key", "")
    if len(api_key) != 32:
        raise ConfigError("智谱 api_key 应为 32 位（open.bigmodel.cn 控制台复制；也可用环境变量 PAI_LLM_KEY）")
    wm = _get(parser, "channels", "wechat_mode", "vision_readonly")
    if wm != "vision_readonly":
        raise ConfigError("wechat_mode 仅允许 vision_readonly（当前 '%s'）：微信通道纯视觉只读，不可更改" % wm)
    return PAIConfig(llm=_build_llm(parser, api_key))
```

## S.2 llm/zhipu_client.py（三通道客户端，节选核心）

```python
"""智谱免费链客户端：fast/deep/vision 三通道 + 自适应限速 + 退避重试。
移植自 We-AIPO zhipu.py（已验证的 _AdaptivePacer 思想）。"""
from __future__ import annotations
import base64, json, time, urllib.request
from tenacity import retry, stop_after_attempt, wait_exponential_jitter

ENDPOINT = "https://open.bigmodel.cn/api/paas/v4/chat/completions"

class _AdaptivePacer:
    """429 时窗口翻倍、连续成功逐步恢复；全局单例，重试绝不放大并发。"""
    def __init__(self, min_interval=0.34):
        self._interval, self._min, self._last, self._ok = min_interval, min_interval, 0.0, 0
    def wait(self):
        delta = time.monotonic() - self._last
        if delta < self._interval:
            time.sleep(self._interval - delta)
        self._last = time.monotonic()
    def on_success(self):
        self._ok += 1
        if self._ok >= 20:
            self._interval = max(self._min, self._interval / 2); self._ok = 0
    def on_rate_limited(self):
        self._interval = min(8.0, self._interval * 2); self._ok = 0

class ZhipuClient:
    def __init__(self, api_key, models):
        self._key, self._models = api_key, models
        self._pacer = _AdaptivePacer()

    @retry(stop=stop_after_attempt(5), wait=wait_exponential_jitter(initial=2, max=30))
    def _post(self, payload):
        self._pacer.wait()
        req = urllib.request.Request(
            ENDPOINT, data=json.dumps(payload).encode("utf-8"),
            headers={"Authorization": "Bearer " + self._key,
                     "Content-Type": "application/json"})
        try:
            with urllib.request.urlopen(req, timeout=120) as resp:
                body = json.loads(resp.read().decode("utf-8"))
        except urllib.error.HTTPError as err:
            if err.code == 429:
                self._pacer.on_rate_limited(); raise
            detail = err.read().decode("utf-8", "ignore")[:200]
            raise RuntimeError("智谱接口错误 %s：%s" % (err.code, detail)) from err
        self._pacer.on_success()
        return body

    def fast(self, prompt, system=None, json_mode=False):
        """系统 1：摘要/分类/格式化。"""
        messages = ([{"role": "system", "content": system}] if system else []) \
                 + [{"role": "user", "content": prompt}]
        payload = {"model": self._models["fast"], "messages": messages, "temperature": 0.3}
        if json_mode:
            payload["response_format"] = {"type": "json_object"}
        body = self._post(payload)
        return {"text": body["choices"][0]["message"]["content"],
                "usage": body.get("usage", {})}

    def deep(self, prompt):
        """系统 2：显式推理链（蒸馏/根因/画像）。"""
        body = self._post({"model": self._models["deep"],
                           "messages": [{"role": "user", "content": prompt}],
                           "thinking": {"type": "enabled"}})
        msg = body["choices"][0]["message"]
        return {"text": msg["content"], "chain": msg.get("reasoning_content", ""),
                "usage": body.get("usage", {})}

    def vision(self, image_path, schema_hint):
        """截图结构化：图片 base64 + schema 约束，json 输出。"""
        with open(image_path, "rb") as fh:
            b64 = base64.b64encode(fh.read()).decode("ascii")
        body = self._post({"model": self._models["vision"],
            "messages": [{"role": "user", "content": [
                {"type": "image_url", "image_url": {"url": "data:image/jpeg;base64," + b64}},
                {"type": "text", "text": "按此 JSON schema 输出，不确定填 unknown：" + schema_hint}]}],
            "temperature": 0.1})
        return {"text": body["choices"][0]["message"]["content"]}
```

## S.3 security/audit.py（只追加审计+哈希链防篡改，全量）

```python
"""audit.db 只追加写入器：全动作留痕，哈希链防篡改。"""
import hashlib, json, sqlite3, threading, time

class Auditor:
    def __init__(self, db_path):
        self._conn = sqlite3.connect(db_path, check_same_thread=False)
        self._conn.execute("""CREATE TABLE IF NOT EXISTS audit (
            id INTEGER PRIMARY KEY AUTOINCREMENT, ts INTEGER NOT NULL,
            actor TEXT NOT NULL, action TEXT NOT NULL, target TEXT,
            detail TEXT, result TEXT NOT NULL, prev_hash TEXT)""")
        self._lock = threading.Lock()
        row = self._conn.execute(
            "SELECT prev_hash FROM audit ORDER BY id DESC LIMIT 1").fetchone()
        self._tail = row[0] if row else "GENESIS"

    def log(self, actor, action, target="", detail=None, result="ok"):
        ts = int(time.time())
        payload = json.dumps({"ts": ts, "actor": actor, "action": action,
                              "target": target, "detail": detail or {},
                              "result": result}, ensure_ascii=True, sort_keys=True)
        digest = hashlib.sha256((self._tail + payload).encode("utf-8")).hexdigest()
        with self._lock:
            self._conn.execute(
                "INSERT INTO audit(ts,actor,action,target,detail,result,prev_hash) "
                "VALUES (?,?,?,?,?,?,?)",
                (ts, actor, action, target, payload, result, digest))
            self._conn.commit()
            self._tail = digest

    def verify_chain(self):
        """完整性自检：重算哈希链，断裂即告警（附录 F Tampering 对策）。"""
        prev, ok = "GENESIS", True
        for (payload, digest) in self._conn.execute(
                "SELECT detail, prev_hash FROM audit ORDER BY id"):
            if hashlib.sha256((prev + payload).encode()).hexdigest() != digest:
                ok = False; break
            prev = digest
        return ok
```

## S.4 使用说明
以上三件即 M0 第 2/3/4 天的开发基座：先让它们通过各自单测（约 25 条），再进入 M1。风格基准：类型注解、显式中文错误、依赖最小（仅 tenacity）、线程安全、可测试（pacer 与 urlopen 皆可 monkeypatch）。后续模块（sense/memory/skills/proactive/learn）全部按此基准编写——本附录代码同时是项目的编码规范样板。


---



# 结语与审批

## 结语
本手册从愿景（1.1 盖茨类比）、理论（第 3 章 29 个思维模型）、技术（第 2/4/5 章+附录 A-N+S 参考代码）、生态（第 6 章+第 11 章竞品解剖）、商业（第 7/9 章+附录 R 推导）、执行（第 8 章+附录 P/P2 逐周演练）到治理（第 10 章+附录 O 生态契约），形成可独立实施的完整闭环。**一个不具备本项目背景的工程师，拿着本手册，可以在 22 周内把它实现出来**——这是"操作手册"标准的自我验证。

## ✋ 审批请求（等待用户决定）
- 批准 → 立即启动 M0 地基（第 8.2 节与附录 P，第一周逐日计划已就绪）；
- 修改 → 指出章节号，按此文档结构定点迭代。
