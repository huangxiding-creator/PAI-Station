# GAP_REPORT — 调研缺口与风险（诚实清单）

## 已知缺口（不阻塞提案，实施期补）

1. **WebSearch 配额尽**（09-26 复位）：社区文章全文未取；已用 metaso+仓库一手资料覆盖核心，dev.to 原文（用户坑三条出处）待复位后补读
2. **Jev 实际月开销未量化**：jev_traj.jsonl 尚未定位到落盘实例（resident 运行期数据目录），当前用量按 E1-E3 实验密度+8 接线位估算；实施 P0 先落 traj 再出真实账
3. **微调数据量**：存量 ~400 决策对 < 深度微调惯常量级；靠 GLM 半自动扩标+周累积补，P2 分期目标（先 1k 后 3k）
4. **单卡 6GB 训练配方未实测**：官方配方 2×T4(32GB)；6GB 需 micro-batch 降+仅 multilingual+可能 LoRA——P2 首个实验即验证

## 风险登记

| 风险 | 等级 | 缓解 |
|---|---|---|
| #185 头输出 300× 尺度 → AMP 数值风险 | 中 | 训练守 notebook 原样 fp32；推理 fp16 须 A/B 验证后再开 |
| #186 温度过拟合 | 中 | held-out 拟合温度（写进 P2 验收） |
| laya 年轻（0.3.6/09-22）API 漂移 | 中 | 锁版本+vendor tarball（已在 _quarantine）+ 升级走隔离评测 |
| GPU 争用（NB/manus 浏览器+3.2GB 常驻） | 中 | 首版只装 multilingual(1.6GB)；训练晚间窗；VRAM 监控熔断 |
| 中文微调不达标 | 中低 | 分期目标+金标准回归门，不过门该位保持原路径（fail-soft 本来就有） |
| 服务单点 | 低 | watchdog 自启（抄 EPC100 配方）+ fail-soft 全链缺席=回原行为 |

## 明确不做（红线）

- 不删 Jev/typesafe 代码与配置（只增引擎位，随时切回）
- 不改任何已钉死的问句措辞（E3 教训）
- 确定性检查（正则/行数/枚举）不交给 laya
- 凭据/轨迹明文不入 git（沿 hash-only 纪律）
