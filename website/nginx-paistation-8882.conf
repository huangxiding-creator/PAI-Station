server {
    listen 8882;
    listen [::]:8882;
    server_name _;
    root /www/PAIStation;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    # HTML 每次回源验证（304 协商缓存），改版即时生效
    location ~* \.html$ {
        add_header Cache-Control "no-cache";
        try_files $uri =404;
    }

    gzip on;
    gzip_types text/html text/css application/javascript image/svg+xml;
    add_header X-Content-Type-Options nosniff;
}
